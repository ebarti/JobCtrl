{
  "name": "@jobctrl/extension",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit --project tsconfig.json",
    "test": "vitest run",
    "build": "vite build",
    "e2e": "corepack pnpm build && vitest run --config vitest.e2e.config.ts"
  },
  "dependencies": {
    "@jobctrl/contracts": "workspace:*"
  },
  "devDependencies": {
    "@jobctrl/tsconfig": "workspace:*",
    "@playwright/test": "^1.50.0",
    "@types/node": "25.9.2",
    "jsdom": "^26.0.0",
    "typescript": "^6.0.3",
    "vite": "^7.3.5",
    "vitest": "^4.1.5"
  }
}
