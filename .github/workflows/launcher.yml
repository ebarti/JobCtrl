name: Native Launcher CI

on:
  push:
    branches: ["main"]
    paths:
      - "launcher/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "scripts/**"
      - "packaging/distribution/**"
      - "workers/automation/pyproject.toml"
      - "workers/automation/src/jobctrl/database.py"
      - "workers/automation/src/jobctrl/infrastructure/migrations/**"
      - "workers/automation/tests/fixtures/shipped_v6_schema.sql"
      - "workers/automation/tests/v6_migration_fixture.py"
      - ".github/workflows/launcher.yml"
  pull_request:
    paths:
      - "launcher/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "scripts/**"
      - "packaging/distribution/**"
      - "workers/automation/pyproject.toml"
      - "workers/automation/src/jobctrl/database.py"
      - "workers/automation/src/jobctrl/infrastructure/migrations/**"
      - "workers/automation/tests/fixtures/shipped_v6_schema.sql"
      - "workers/automation/tests/v6_migration_fixture.py"
      - ".github/workflows/launcher.yml"
  workflow_dispatch:

jobs:
  launcher:
    name: Darwin launcher (vet + race)
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pinned Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.4"
          cache-dependency-path: launcher/go.mod

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22.21.1"
          cache: pnpm

      - name: Set up pinned uv
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2
        with:
          version: 0.11.7

      - name: Set up migration integration Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.13"
          cache: pip
          cache-dependency-path: workers/automation/pyproject.toml

      - name: Install migration integration package
        run: python -m pip install ./workers/automation

      - name: Install Node dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Vet native launcher
        working-directory: launcher
        run: go vet ./...

      - name: Test native launcher
        working-directory: launcher
        run: go test -race ./...

      - name: Test pinned distribution build contracts
        run: corepack pnpm scripts:test

      - name: Audit distribution contracts
        run: corepack pnpm distribution:audit
