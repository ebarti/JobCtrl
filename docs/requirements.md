# Requirements

This document is JobCtrl's current software requirements catalog: the
invariants that must stay true as the implementation changes. It is split into
**behavioral requirements** (`BR-*`) — what the system should do for users — and
**technical requirements** (`TR-*`) — the implementation constraints behind them.
Each ID is a stable handle that is never reused, so code, PRs, and QA can cite a
requirement by number.

Every requirement is one row in the tables below. The `Added` column is the date
the requirement first appeared in this file's git history, not the date it was
last edited; the `Related docs` column points at the current owning
documentation, not the historical plan that first proposed it.

## Behavioral Requirements

### Product Safety

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-001 | 2026-05-22 | JobCtrl must never submit applications, run destructive profile/database actions, or bypass third-party controls unless the user explicitly authorizes that behavior; this includes browser submission, CAPTCHA, paywall, login, rate-limit, and bot-control bypass. | Job seekers. | Keep automation safe, consent-driven, and compliant with site policies. | [README](../README.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-050 | 2026-07-04 | JobCtrl must enforce a configurable daily LLM spend ceiling: every workflow that spends LLM tokens runs a budget preflight and stops with a non-retryable budget error once the day's estimated spend reaches the ceiling, and the over-budget state must be surfaced to the user. | Job seekers. | Prevent runaway LLM cost during automated runs. | [pipeline operations](architecture/pipeline/operations.md), [configuration](user/configuration.md) |
| BR-054 | 2026-07-04 | Automated apply submission must be at-most-once: run claiming must exclude running, succeeded, and needs-verification runs; a submit-intent checkpoint must be recorded immediately before an owned submit mediator commits; and a crash after submit intent must park the run for human verification instead of retrying. | Job seekers. | A duplicate application is worse than a missed retry. | [stage walkthrough](architecture/pipeline/stages.md), [security](user/security.md) |
| BR-057 | 2026-07-09 | Users must be able to enable or disable public and licensed compensation sources from Settings. Levels.fyi public access must work without credentials by reading its provider-published job-family/location Markdown or equivalent public structured page data, must preserve Levels.fyi attribution, and must not use private endpoints or bypass login, paywall, rate-limit, or other provider controls. Licensed modes must remain available for authorized feeds and require Europe coverage confirmation. The API and worker must consume the same persisted preference. | Job seekers. | Let users benefit from provider-authorized public compensation data while preserving source control, provenance, and compatibility with licensed access. | [configuration](user/configuration.md), [local TypeScript API](local-ts-api.md), [read model](architecture/read-model.md) |
| BR-058 | 2026-07-12 | A user must be able to unlock every core AI stage with any one ready Codex, Claude, or Google provider; connecting multiple providers may be recommended for ensemble diversity but must not be mandatory. Direct raw-OpenAI and local/custom HTTP routes are not release providers. | Job seekers configuring first run. | Keep launch setup low-friction and vendor-neutral without claiming an incomplete provider path. | [configuration](user/configuration.md), [runtime boundaries](architecture/runtime.md) |
| BR-059 | 2026-07-12 | After a provider is ready, users must be able to choose and clear a provider-scoped preferred model from the sanitized live catalog returned by that same authenticated Codex, Claude, or Google runtime; a saved choice must never select a different provider, and JobCtrl must not claim that a model is available when the active runtime did not advertise it. | Job seekers controlling model behavior. | Offer meaningful model control without leaking credentials, maintaining a stale duplicate model registry, or changing provider routing implicitly. | [configuration](user/configuration.md), [Profile & Settings API](api/profile-and-settings.md) |
| BR-060 | 2026-07-15 | Browser discovery must remain read-only and path-free; optional browser adoption requires exactly one explicit detected-browser ID or write-only executable path, and a stale detected ID must fail closed without fallback. | Job seekers enabling browser capabilities. | Finding software on the host is not consent to launch, copy, persist, or adopt it. | [Profile & Settings API](api/profile-and-settings.md), [security](user/security.md) |
| BR-063 | 2026-07-20 | Before every live application claim, JobCtrl must block a confirmed prior application to the same canonical job or accepted duplicate identity and require an explicit, evidence-bound, reasoned, one-attempt confirmation for a confirmed application to the same employer and a materially equivalent role. Only canonical identity and confirmed application facts may establish history; distinct roles, similar-employer false positives, dry runs, pending suggestions, notes, failed pre-submit attempts, and unverified assumptions must not. The prior evidence, decision, override, and consumption must remain inspectable and auditable. | Job seekers. | Prevent unintended repeat applications without suppressing legitimate applications to different roles. | [Apply](user/apply.md#repeat-application-protection), [storage](architecture/storage.md), [security](user/security.md#repeat-applications-fail-closed) |
| BR-064 | 2026-07-29 | Untrusted page content and the model that reads it must never own final browser-submit authority. Until a trusted canonical final-form manifest and a one-shot mediator exist below the model, live browser-form runs must stop before prompt rendering, browser launch, or agent execution and require manual completion. Direct use-case, saga, and adapter calls must enforce the same fail-closed boundary. | Job seekers. | A prompt-injected page must not be able to turn generic browser click authority into an employer submission. | [Apply](user/apply.md#final-browser-submission-is-manual), [stage walkthrough](architecture/pipeline/stages.md), [developer security](developer/security.md#apply-path-containment) |

### Operations And Workflow

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-002 | 2026-05-22 | Every job must expose explicit durable state for discovery, enrichment, scoring, tailoring, cover-letter, PDF, and apply stages, including blocked, failed, skipped, exhausted, canceled, stale, and running states where applicable. | Job seekers and operators. | Show what happened, what is blocked, and what can be retried without reading the database. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-003 | 2026-05-22 | Failed retryable stages must be retryable without rerunning the entire pipeline, and retrying one stage must not silently reset unrelated stage progress. | Job seekers. | Recover from local failures without duplicating work or losing progress. | [pipeline architecture](architecture/pipeline/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-004 | 2026-05-22 | Workflow execution must record event history, run identifiers, workflow identifiers, and current status for user-triggered and automated work. | Job seekers and maintainers. | Make long-running work observable, cancellable where possible, and debuggable. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-005 | 2026-05-22 | Apply automation must expose clear status, logs, next action, retry options, dry-run state, and timeout failures per job. | Job seekers. | Make high-risk application steps inspectable before and after automation runs. | [pipeline architecture](architecture/pipeline/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-006 | 2026-05-22 | The web app must show dashboard, jobs, artifacts, profile, workflow-run, and apply-run views backed by the TypeScript API. | Job seekers operating their search. | Provide a usable local product instead of requiring command-line or database work. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md) |
| BR-007 | 2026-05-22 | The operations dashboard must show stuck, failed, blocked, ready, active, and applied work, plus recent activity and active runs. | Job seekers and operators. | Prioritize the next useful action during a job search. | [local TypeScript API](local-ts-api.md) |
| BR-008 | 2026-05-22 | Job detail views must show stage state, event history, artifacts, score detail, apply status, and safe available actions. | Job seekers. | Let users understand and control each job from one place. | [pipeline architecture](architecture/pipeline/index.md), [local TypeScript API](local-ts-api.md) |
| BR-009 | 2026-05-22 | Lists must support stable filtering, sorting, pagination, deep links, and selected-detail workspace state while background work updates local data. | Job seekers reviewing many jobs. | Keep navigation predictable as local data changes. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md) |
| BR-010 | 2026-05-22 | UI actions must call structured API actions where backend support exists, returning accepted command state and run identifiers instead of relying only on copied shell commands. | Job seekers. | Provide real product controls for actions that the API owns. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-066 | 2026-07-29 | Discover, JobPreparation, and Apply executions must share one workflow-run history, status vocabulary, timeline, and idempotent cancellation experience, including truthful terminal state after a cancel request. | Job seekers and operators. | Make every long-running product stage equally inspectable and controllable. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-056 | 2026-07-05 | Browser-extension assisted autofill must be available on ordinary HTTP(S) application forms without a fixed ATS-host allowlist, use deterministic profile-backed suggestions that the user reviews and accepts, remain passive until explicitly invoked, and never submit applications. HTTP(S) host access is wildcarded for both those pages and explicit brokered Discovery API tasks; capture/autofill API calls remain loopback-only and the service worker must not originate remote traffic without a leased task. | Job seekers filling application forms. | Support application and source sites without per-host releases while preserving user control, bounded live-profile Discovery, local capture/autofill boundaries, and non-submission. | [README](../README.md), [security](user/security.md), [local reliability QA](local-reliability-qa.md) |
| BR-061 | 2026-07-15 | The pipeline operations view must distinguish immutable Discover execution lineage, current-run work, its explicit existing-backlog sweep, global work, source-family progress, reconciliation, capacity, bounded active work, freshness, and typed ETA instead of presenting one blended completion percentage. | Job seekers and operators. | Make ownership, remaining work, and uncertainty inspectable without implying that source completion means pipeline completion. | [Operations & Events API](api/operations-and-events.md), [pipeline operations](architecture/pipeline/operations.md) |

### Discovery And Enrichment

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-011 | 2026-05-22 | Discovery must prioritize the user's configured target roles, locations, seniority, tracks, and work models. | Job seekers. | Focus results on the user's active search instead of broad generic scraping. | [local TypeScript API](local-ts-api.md) |
| BR-012 | 2026-05-22 | Saved target-search locations must be validated as real places before they can drive discovery. | Job seekers configuring searches. | Prevent invalid searches from polluting results. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-013 | 2026-05-22 | Hybrid and on-site discovery must filter exclusively for the configured target location. | Job seekers. | Keep location-bound results relevant to where the user can work. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-014 | 2026-05-22 | Remote discovery must filter for the configured target country or region. | Job seekers. | Keep remote results aligned with the user's legal and practical work target. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-015 | 2026-05-22 | Remote discovery for European target countries must also include jobs remote in Europe. | Job seekers. | Capture regional remote roles that are relevant even when not tied to one country. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-016 | 2026-05-22 | Profile-driven target discovery must search at least the last 30 days unless local configuration sets a larger lookback window. | Job seekers. | Avoid stale searches while allowing broader local search windows when requested. | [local TypeScript API](local-ts-api.md) |
| BR-017 | 2026-05-22 | Aggregators and broad boards may provide leads, but JobCtrl must prefer canonical employer, ATS, API, licensed feed, or official posting sources before downstream scoring and automation. | Job seekers. | Improve relevance, freshness, and applyability of jobs. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-018 | 2026-05-22 | Source quality must be observable and support trusted, normal, experimental, quarantined, and disabled states. | Job seekers and maintainers. | Stop low-quality sources from degrading the pipeline. | [local reliability QA](local-reliability-qa.md) |
| BR-019 | 2026-05-22 | Manual capture must be available for useful leads that cannot be fetched safely, including user-provided URLs, saved HTML, pasted text, browser-extension active-page captures, or email-derived posting content. | Job seekers. | Allow user-mediated progress without bypassing third-party protections. | [README](../README.md), [pipeline architecture](architecture/pipeline/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-020 | 2026-05-22 | Discovery and enrichment must preserve provenance, source observations, canonical URLs, apply URLs, active state, full descriptions, snapshots, and source-native identifiers where available. | Job seekers and maintainers. | Make jobs auditable, deduplicable, and useful for scoring and materials. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-021 | 2026-05-22 | Deduplication must preserve source observations and avoid silently merging uncertain fuzzy matches; uncertain matches must be reviewed or quarantined. | Job seekers. | Reduce duplicate clutter without losing evidence or creating bad merges. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-065 | 2026-07-29 | Every job must have an immutable system-generated JobId independent of posting and application URLs; migration must preserve all canonical references, while source board and employer remain separate facts. | Job seekers and maintainers. | Keep job history stable across URL changes and prevent source/employer inference from corrupting identity or filters. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [DDD target](architecture/domain-model/tactical.md) |
| BR-042 | 2026-05-30 | Target search must be configured from the Discovery page, not general Preferences, and must expose normalized IC, management, and executive tracks plus one role-area-independent seniority ladder: Junior IC, Mid IC, Senior IC, Staff IC, Principal IC, Manager, Senior Manager, Director, VP, SVP, and C-Level. VP, SVP, and C-Level must remain distinct floors. | Job seekers configuring searches. | Keep discovery-specific controls next to discovery operations, separate career scope from role area, and prevent ambiguous free-form track/seniority input. | [Discovery](user/discovery.md), [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-043 | 2026-05-30 | Discovery must move postings verified unavailable, expired, removed, or location-incompatible to a Closed list that remains inspectable but is excluded from active dashboards and worker queues. | Job seekers reviewing discovered jobs. | Keep stale postings from polluting active work while preserving evidence about why they disappeared. | [README](../README.md), [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-044 | 2026-05-30 | Pipeline progress text must describe whether a stage is running, complete, failed, or ready to run again without exposing prior-worker recovery implementation details as the primary user message. | Job seekers running local pipelines. | Make long-running discovery/apply work understandable and actionable from the Pipelines page. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-051 | 2026-07-04 | Scheduled discovery must be off by default; enabling a recurring discovery schedule must be an explicit operator action. | Job seekers. | Background automation and its cost must never be a surprise. | [pipeline operations](architecture/pipeline/operations.md) |
| BR-062 | 2026-07-17 | An interrupted broad-board source activity must resume unfinished work under the same Discover execution without losing accepted postings or restarting the run-wide new-job limit; replay must be harmless, recovered work must be visible, and explicit cancellation must leave unfinished search units terminal rather than stale/resumable. | Job seekers and operators. | Long discovery runs must recover from worker loss without duplicating results or confusing a user-requested stop with a crash. | [discovery](user/discovery.md), [pipeline envelope](architecture/pipeline/envelope.md), [local reliability QA](local-reliability-qa.md) |
| BR-069 | 2026-09-02 | Every job-source acquisition owned by integrated Discovery—including broad-board provider sessions, ATS/API and Workday requests, Smart Extract renders, robots reads, and detail enrichment—must use one explicitly selected extension installation in the user's currently running Chrome profile. Another profile holding the token must not race for leases. Discovery must never copy or separately launch that profile and must reject or fail closed when the live extension is unavailable instead of using a direct-network, Playwright, adopted-browser, or copied-profile fallback. A LinkedIn detail request made inside that owner-authenticated live session must not be blocked by the anonymous crawler's robots verdict; public-URL validation, exact-origin controls, pacing, budgets, no-submit boundaries, and audit history remain mandatory. In-flight work must have independent heartbeat, cancellation, hard-timeout/tab-cleanup, bounded-concurrency/backpressure, exact-origin redirect, and UTF-8 byte-limit controls. | Job seekers running Discovery. | Reuse the user's actual current site sessions and profile changes while making browser ownership and failure behavior explicit and reliable. | [Discovery](user/discovery.md), [runtime](architecture/runtime.md), [local TypeScript API](local-ts-api.md) |

### Single Discovery Preparation Stage

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-034 | 2026-05-27 | Discovery must be the single user-facing preparation stage for finding, enriching, scoring, and tailoring eligible jobs; scoring and tailoring may still appear in job detail, diagnostics, and maintenance actions, but not as Jobs-list substages. | Job seekers. | Keep the product workflow simple without flattening the internal domain model. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-035 | 2026-05-27 | Tailoring must run only for jobs whose latest persisted score, hard-blocker state, active state, and current score threshold make them eligible. | Job seekers. | Prevent irrelevant or blocked jobs from consuming tailoring work or producing misleading artifacts. | [pipeline architecture](architecture/pipeline/index.md), [tailoring](architecture/tailoring.md) |
| BR-036 | 2026-05-27 | Scoring policy, algorithm, prompt, rubric, parser, and correction-derived anchor changes must be versioned and rollbackable; new Discovery work uses the current scoring policy while existing jobs keep their recorded scoring policy version until explicitly rescored. | Job seekers and maintainers. | Make scoring changes auditable and reversible without silently rewriting recorded decisions. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-037 | 2026-05-27 | Users must be able to rescore one job with the current scoring policy and rescore all active jobs whose latest score was not produced by the current scoring policy. | Job seekers. | Let users adopt policy changes deliberately at per-job or backlog scale. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-038 | 2026-05-27 | Score threshold changes must apply to all discovered jobs: lowering the threshold must enqueue missing tailoring work for newly eligible jobs, and raising the threshold must soft-suppress active tailored artifacts for newly ineligible jobs. | Job seekers. | Keep tailoring eligibility consistent with the user's live preference without rescoring jobs. | [local reliability QA](local-reliability-qa.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-039 | 2026-05-27 | Tailoring prompt and configuration changes must be versioned and rollbackable; new tailoring work uses the current tailoring policy while existing artifacts keep their recorded tailoring policy version until explicitly re-tailored. | Job seekers and maintainers. | Make generated materials auditable and avoid silently replacing already reviewed artifacts. | [pipeline architecture](architecture/pipeline/index.md), [tailoring](architecture/tailoring.md) |
| BR-040 | 2026-05-27 | Users must be able to re-tailor one job with the current tailoring policy and re-tailor all active eligible jobs whose latest active tailored artifact was not produced by the current tailoring policy. | Job seekers. | Let users adopt tailoring prompt/configuration changes deliberately at per-job or backlog scale. | [local TypeScript API](local-ts-api.md), [tailoring](architecture/tailoring.md) |
| BR-041 | 2026-05-27 | Soft-suppressed tailored artifacts must be hidden from active artifact displays and downstream apply readiness while remaining preserved as audit artifacts unless the user explicitly deletes them. | Job seekers and maintainers. | Keep active workflow surfaces truthful without destroying generated evidence. | [local reliability QA](local-reliability-qa.md), [pipeline architecture](architecture/pipeline/index.md) |

### Scoring

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-022 | 2026-05-22 | Scoring must be an applicant-side decision aid, not an autonomous hiring or application decision. | Job seekers. | Help users prioritize while preserving human judgment. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-023 | 2026-05-22 | Auto-apply must never be gated by score alone; hard blockers, missing materials, safety policy, and user approval requirements must still apply. | Job seekers. | Prevent high scores from overriding blockers, missing materials, or safety policy. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-024 | 2026-05-22 | Persisted fit scores must be deterministic outputs of a versioned scoring policy over structured evidence, including technical fit, experience/seniority fit, and role fit, plus a requirement-fit dimension resolved from employer-analysis requirement assessments when they are present. | Job seekers and maintainers. | Make scores comparable, explainable, and reproducible. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-025 | 2026-05-22 | LLM scoring calls may extract evidence, gaps, blockers, transferable signals, confidence, and rationale, but must not be the final scoring authority. | Job seekers. | Reduce unsupported model opinion in decisions that affect applications. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-026 | 2026-05-22 | Low confidence must mean review needed, not a disguised low score, and missing evidence must reduce confidence even when a model proposes high fit. | Job seekers. | Distinguish weak evidence from poor fit. | [pipeline architecture](architecture/pipeline/index.md) |
| BR-027 | 2026-05-22 | User score corrections must immediately affect future scoring, become calibration anchors, and mark comparable uncorrected scores stale until the user explicitly rescores them. | Job seekers. | Let the system learn from explicit user judgment without silently rewriting history. | [local TypeScript API](local-ts-api.md) |
| BR-028 | 2026-05-22 | Score details must expose fit, band, blockers, confidence, evidence, gaps, dimensions, policy version, stale state, and correction trace. | Job seekers. | Explain why a job is recommended, questionable, or blocked. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-067 | 2026-07-29 | Learning from user feedback must preserve the shipped score-correction anchors and approved role-match exclusions, add structured tailoring-review learning through inspectable recommendations, and require an explicit user action before creating a versioned policy or preference revision; existing scores, artifacts, profile facts, thresholds, and Apply decisions remain unchanged until their normal explicit recomputation path runs. | Job seekers. | Personalize future work without duplicating existing feedback behavior or silently rewriting user data, historical decisions, or application eligibility. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [outcomes and feedback](user/outcomes-and-feedback.md) |

### Materials, Profile, And Artifacts

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| BR-029 | 2026-05-22 | Profile data, resume style, templates, generated materials, and artifacts must remain local by default. | Job seekers. | Preserve privacy for sensitive career material. | [pipeline architecture](architecture/pipeline/index.md), [architecture](architecture/index.md) |
| BR-030 | 2026-05-22 | Profile editing must preserve existing profile, resume style, resume template, and import behavior until stable replacements are implemented. | Job seekers. | Avoid losing user configuration during architecture changes. | [local TypeScript API](local-ts-api.md) |
| BR-031 | 2026-05-22 | Cover letters must require tailored-resume prerequisites before generation. | Job seekers. | Prevent inconsistent or unsupported application material. | [local reliability QA](local-reliability-qa.md), [pipeline architecture](architecture/pipeline/index.md) |
| BR-032 | 2026-05-22 | Server-side PDF generation must operate only on known DB-backed material artifacts. The sole editor-export exception may render only the currently mounted Plate document in the browser to a user download; its visible layer must preserve the browser-rendered glyphs, spacing, line wrapping, template styling, and A4 or Letter geometry, while retaining an extractable text layer. The export action must not accept a local path, save editor state, register or replace an artifact, or change approval state. Profile may separately project unambiguous canonical text edits from its Plate surface into the existing validated form draft; formatting and composite rendered lines remain local. | Job seekers. | Allow explicit, visually faithful export of live edits without creating an unrelated-file conversion path or confusing a local copy with an accepted artifact, while keeping directly edited candidate facts in the canonical Profile workflow. | [candidate profile](user/candidate-profile.md), [local reliability QA](local-reliability-qa.md), [pipeline architecture](architecture/pipeline/index.md), [frontend patterns](architecture/frontend/patterns.md) |
| BR-033 | 2026-05-22 | Artifacts must be visible, inspectable, previewable where supported, and openable only through known local artifact metadata. | Job seekers. | Make generated outputs useful while avoiding unsafe file access. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-045 | 2026-05-30 | Editable profile, preferences, discovery-target, and general settings forms must autosave five seconds after the last edit and support Ctrl+Z/Cmd+Z undo for setting controls that do not have native text undo. | Job seekers editing local settings. | Prevent accidental loss of search configuration while keeping rapid checkbox/select changes reversible. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-046 | 2026-06-24 | Apply Review must render current HTML/CSS-generated resumes from the same generated HTML source that prints the final PDF, using a Plate-backed resume surface with line selection and JobCtrl audit annotations inside the resume instead of a side-by-side PDF/image overlay. | Job seekers reviewing tailored materials. | Let users inspect the final resume layout and audit comments in one faithful document surface before approval. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-047 | 2026-06-24 | Approved LaTeX resume artifacts must remain inspectable and have a safe conversion or refresh path onto the HTML/CSS resume source when needed. | Job seekers with existing generated materials. | Preserve existing artifacts while keeping review and future editing on the same render source. | [README](../README.md), [local reliability QA](local-reliability-qa.md) |
| BR-048 | 2026-06-26 | Resume template editing must let the user save style/layout templates, choose a default template, and override the template per job without persisting candidate profile data or job facts into the template payload. | Job seekers tailoring visual presentation. | Let users control resume appearance while preserving profile-data ownership and privacy. | [local TypeScript API](local-ts-api.md), [frontend target](architecture/frontend/index.md), [local reliability QA](local-reliability-qa.md) |
| BR-049 | 2026-06-30 | Resume tailoring must plan coverage from job requirements to existing profile achievements before writing, preserve pinned and requirement-covered content even when it exceeds the normal bullet budget, and expose bounded Apply Review audit summaries for coverage, gaps, claim labels, revision decisions, and review blockers without leaking raw prompts, full profile/job text, local paths, generated PDFs, logs, browser data, or SQLite contents. | Job seekers reviewing tailored materials. | Make generated resumes explainable and reviewable while keeping profile evidence and local artifacts private. | [tailoring](architecture/tailoring.md), [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-052 | 2026-07-04 | Apply Review must support interactive resume revision — draft edits, named revisions, and comment threads — with draft-aware approval, and revising must never destroy the last accepted artifact. | Job seekers reviewing materials. | Let users correct materials safely before submission. | [materials audit](architecture/materials.md), [local TypeScript API](local-ts-api.md) |
| BR-068 | 2026-08-20 | The saved Candidate Profile experience sequence must be the presentation order for baseline and tailored resumes. Profile editing must provide explicit move-up, move-down, and newest-first controls through the normal validated save path; renderers must not silently re-sort the sequence, and roles without bullets must not reserve empty-list spacing. | Job seekers maintaining resume history. | Make resume order deliberate, persistent, and visually predictable without adding a second ordering store. | [candidate profile](user/candidate-profile.md), [frontend patterns](architecture/frontend/patterns.md), [local reliability QA](local-reliability-qa.md) |

## Technical Requirements

### Product Surface And Runtime

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-001 | 2026-05-22 | JobCtrl must use the TypeScript API, React web app, and Python automation engine as its supported product surface. | Users and maintainers. | Provide one clear supported architecture. | [architecture](architecture/index.md), [local TypeScript API](local-ts-api.md) |
| TR-002 | 2026-05-22 | Hosted, multi-tenant, billing, managed browser, managed workflow, and cloud-storage features must stay out of local mode until explicit scale or product triggers are met. | Maintainers and future hosted users. | Protect the local product from premature SaaS complexity. | [architecture](architecture/index.md), [DDD target](architecture/domain-model/index.md), [frontend target](architecture/frontend/index.md) |
| TR-003 | 2026-05-22 | A source checkout must run its Temporal dev server, TypeScript API, React/Vite web app, and Python worker through one documented developer command, with `corepack pnpm dev` as the full attached source-stack entry point. | Contributors and source-checkout operators. | Keep the contributor path reproducible without confusing it with the installed product command surface. | [README](../README.md), [local development](local-development.md), [package scripts](../package.json) |
| TR-004 | 2026-05-22 | The TypeScript API must expose typed local JSON endpoints for health, dashboard, jobs, artifacts, profile, settings, workflow runs, job events, event streaming, and actions. | The React app and local automation users. | Give the UI stable contracts instead of relying on SQLite inspection or shell output. | [local TypeScript API](local-ts-api.md), [architecture](architecture/index.md) |
| TR-005 | 2026-05-22 | The API must use local-only defaults, including the documented loopback API address, restrictive browser access, and token-gated browser-extension routes that cannot bypass the loopback Host gate. | Local users. | Avoid accidentally exposing private job-search data outside the machine. | [local TypeScript API](local-ts-api.md) |
| TR-006 | 2026-05-22 | SQLite must remain the local source of truth until a hosted or scale trigger requires another database, and API, SSE, and worker activities must agree on the active database path. | Local users and maintainers. | Preserve a simple, inspectable, portable local data model. | [architecture](architecture/index.md), [local TypeScript API](local-ts-api.md) |
| TR-034 | 2026-07-10 | A published native distribution must give curl and Homebrew the same signed platform artifact and the same native `jobctrl` executable; `jobctrl start` and domain commands must work from any directory without a source checkout or user-installed Git, Node, pnpm, Corepack, uv, Python, Temporal, Poppler, Playwright, or npx. Its core payload must contain one managed Playwright Chromium headless shell and require system Chrome/Chromium only after explicit capability enablement. | Installed local users and release maintainers. | Keep acquisition method independent from app lifecycle and CLI behavior while avoiding an unnecessary full browser or contributor toolchain. | [bundled distribution plan](plans/implemented/2026-07-10-bundled-jobctrl-distribution-plan.md), [runtime boundaries](architecture/runtime.md), [distribution ADR](decisions.md) |
| TR-035 | 2026-07-12 | Guided provider configuration must expose only Codex, Claude, and Google; replace or revoke a provider without partial Keychain state; never return or log stored secrets; and require verified provider credentials rather than non-secret project metadata alone. | Users and maintainers. | Prevent accidental account routing, false readiness, and secret leakage during first-run setup. | [runtime boundaries](architecture/runtime.md), [security](user/security.md), [Profile & Settings API](api/profile-and-settings.md) |
| TR-036 | 2026-07-12 | Preferred model writes must validate each non-null ID against the live catalog returned by the current ready-provider runtime, persist only provider/model IDs in `config.json`, and resolve new adapters in the order explicit non-default workflow model, selected-provider preference, provider default while preserving Claude/Codex/Google default-provider order. | Users and maintainers. | Keep selection deterministic, provider-scoped, current with the executing runtime, and separate from credential storage. | [runtime boundaries](architecture/runtime.md), [configuration](user/configuration.md), [local TypeScript API](local-ts-api.md) |

### Architecture And State

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-007 | 2026-05-22 | JobCtrl must model its domain through the Pipeline, Profile, Scoring, Materials, Discovery, Enrichment, Apply, and Operations contexts. | Maintainers. | Keep product behavior organized around stable domain boundaries. | [DDD target](architecture/domain-model/index.md), [architecture](architecture/index.md) |
| TR-008 | 2026-05-22 | Temporal must orchestrate local long-running work: `JobPipelineWorkflow` for batch stage runs, `DiscoverWorkflow` for discovery, `JobPreparationWorkflow` for per-job preparation, `ApplyWorkflow` for apply automation, and `ProfileImportWorkflow` / `CompensationRefreshWorkflow` for heavy maintenance commands. | Job seekers running local automation. | Provide durable workflow execution and cancellation for work that can outlive a single request. | [pipeline architecture](architecture/pipeline/index.md), [architecture](architecture/index.md) |
| TR-009 | 2026-05-22 | JobCtrl must publish local domain events to projection-backed read models with resumable projection state. | The API, UI, and maintainers. | Keep operational views accurate without coupling them to write internals. | [architecture](architecture/index.md), [frontend target](architecture/frontend/index.md) |
| TR-010 | 2026-05-22 | The web app must receive local event updates through the API's `GET /v1/events/stream` SSE stream. | Job seekers monitoring active work. | Reflect worker progress without disruptive full-page refreshes. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md) |
| TR-011 | 2026-05-22 | Realtime updates must preserve list context such as filters, selection, scroll position, and loading state where practical, using targeted updates where available and invalidation otherwise. | Job seekers reviewing and triaging jobs. | Prevent background workflow progress from interrupting active review. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md) |
| TR-012 | 2026-05-22 | The frontend must keep server state, URL state, and client-only state separate through TanStack Query, TanStack Router, and client-state stores or context. | Maintainers and users. | Avoid stale UI behavior and make workflows shareable or restorable through URLs. | [frontend target](architecture/frontend/index.md), [decisions](decisions.md) |
| TR-029 | 2026-05-30 | Settings autosave and undo must reuse the existing TanStack Form validation and mutation paths, debounce saves for five seconds after edits, clear pending saves on reset/unmount, and keep an in-session bounded undo history for non-text setting controls. | Maintainers and users. | Preserve one source of validation/persistence behavior while making settings edits resilient. | [frontend target](architecture/frontend/index.md), [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| BR-053 | 2026-07-04 | A local backup command must produce a consistent copy of the SQLite database, and schema migrations must be guarded by a schema-version check. | Job seekers and maintainers. | Protect local data across upgrades and mistakes. | [storage](architecture/storage.md), [README](../README.md) |
| TR-033 | 2026-07-04 | The TypeScript API and the Python worker must communicate over the local JSON-RPC protocol at the internal RPC route, and worker-backed API actions must be gated on a healthy worker heartbeat. | Maintainers. | Keep the TS↔Python boundary explicit, typed, and observable. | [local TypeScript API](local-ts-api.md), [runtime boundaries](architecture/runtime.md) |
| TR-037 | 2026-07-15 | Frontend primitives must use owned shadcn Rhea/Base wrappers in `shared/ui/`; feature code must not import Radix directly, and migration-boundary plus wrapper tests must enforce the Base UI seam. | Frontend maintainers. | Keep primitive behavior, accessibility, styling, and future migrations inside one reviewable boundary. | [frontend patterns](architecture/frontend/patterns.md), [frontend testing](architecture/frontend/testing.md), [decisions](decisions.md) |
| TR-038 | 2026-07-15 | A retry-stage request with `runAfter: true` must verify worker readiness before resetting stage state or appending reset events; an unavailable worker returns 503 and preserves the prior stage and event history. | Users and maintainers. | A failed dispatch prerequisite must not destroy the evidence or retryability of the accepted failed state. | [complete API contract](api/complete-contract.md), [runtime boundaries](architecture/runtime.md) |
| TR-039 | 2026-07-15 | Pipeline operations telemetry must count every active slot while exposing only a bounded allowlisted detail list, an exact allowlisted total/truncation state when knowable, safe workflow/run references, non-reversible opaque IDs, and the minimum display metadata needed for recognition (job title and company). Activity arguments, URLs, paths, full profile or job-posting content, prompts, provider output, credentials, payloads, and exception text must not enter the broad read model. | Users and maintainers. | Operational visibility must not create a new sensitive-data export path or disguise omitted work as a complete list. | [observability](architecture/observability.md), [complete API contract](api/complete-contract.md) |
| TR-040 | 2026-07-15 | Event producers, TypeScript/Python registries, projection folds, and frontend invalidation handlers must enforce membership and coverage through registry-derived parity tests rather than documentation-maintained event counts. | Maintainers. | Event taxonomies evolve; executable parity prevents silent projection or UI freshness gaps without brittle prose totals. | [data, events, and projections](architecture/data-events-and-projections.md), [frontend realtime](architecture/frontend/realtime.md) |
| TR-041 | 2026-07-17 | Resumable broad-board discovery must persist an immutable query/location/board plan under the exact Discover workflow/run identity; fence every checkpoint and result write by Temporal activity attempt/lease epoch; commit idempotent accepted-job or hashed filtered-result receipts before explicit JobStreaming acknowledgement; derive progress and limits from those receipts; and fail request-fingerprint or cursor-schema incompatibility explicitly. | Maintainers and operators. | Provider cursors alone cannot prove JobCtrl consumed or filtered a posting, and activity retry alone cannot prevent a delayed old worker from overwriting newer work. | [runtime boundaries](architecture/runtime.md), [storage](architecture/storage.md), [pipeline envelope](architecture/pipeline/envelope.md) |
| TR-042 | 2026-07-29 | Canonical job storage must use tenant-scoped JobId identity with posting URL as a unique locator. The local upgrade must stop the old runtime, back up the paired SQLite/Temporal state, run one transactional v6-to-v7 migration that preserves every foreign reference, verify it, and start only the exact v7 runtime. URL resolution is allowed only in that migration or at an explicit user/API/import locator boundary; current repositories, events, projections, and workflow contracts must neither inspect legacy schema shapes nor emit URL-shaped identity. | Maintainers and local users. | Protect local history and reference integrity with one deterministic forward upgrade rather than rolling-deployment compatibility machinery. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [storage](architecture/storage.md) |
| TR-043 | 2026-07-29 | Projection builders must source score, application, status, and artifact state only from canonical relations; expose only DB-backed artifacts; persist source and employer separately; and index normalized scoring keywords by tenant, JobId, and score version. | Maintainers and local users. | Remove fabricated or inferred read-model state and support truthful indexed filtering. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [read model](architecture/read-model.md) |
| TR-044 | 2026-07-29 | Realtime handlers must patch exact tenant-scoped job, artifact, dashboard, and workflow-run queries when event data is sufficient, use bounded invalidation otherwise, and remain covered by registry-derived parity plus patch/rollback tests. | Frontend maintainers and users. | Keep live views current without losing list context or causing avoidable reloads. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [frontend realtime](architecture/frontend/realtime.md) |
| TR-045 | 2026-07-29 | The feedback-learning contract must keep scoring, Discovery, and Materials revision ownership separate; project only allowlisted structured facts rather than raw summaries or source text; derive tailoring recommendations from at least three compatible accepted signals across two jobs with no unresolved contradiction; support correction, tombstoning, re-derivation, retention, and rollback; and pass deterministic quality/privacy fixtures before acceptance is enabled. Outcome associations are sample-gated and cannot alone generate a policy recommendation. | Maintainers and local users. | Keep personalization explainable, privacy-bounded, evaluable, and honest about causality. | [active delivery plan](plans/2026-07-29-stable-job-identity-workflow-feedback-learning.md), [outcomes and feedback](user/outcomes-and-feedback.md) |

### Preparation Orchestration

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-022 | 2026-05-27 | The integrated Discovery preparation workflow must remain event-driven through domain events, projection-backed read models, and idempotent preparation work items rather than UI-driven chaining. | Maintainers. | Keep long-running preparation restartable, observable, and decoupled across bounded contexts. | [architecture](architecture/index.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-023 | 2026-05-27 | Score, tailor, and artifact-suppression work selection must use indexed pending/outdated work queues or equivalent bounded selectors, not repeated unbounded full-table scans. | Maintainers and future hosted users. | Let the local SQLite design scale to larger job backlogs and map cleanly to a hosted queue later. | [DDD target](architecture/domain-model/index.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-024 | 2026-05-27 | Every score and active tailored artifact must persist the scoring policy or tailoring policy version that produced it. | Maintainers and job seekers. | Make version drift, rollback, and explicit recomputation decisions inspectable. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-025 | 2026-05-27 | Score-threshold changes must recompute tailoring eligibility from persisted score and blocker data without invoking the scoring LLM. | Maintainers and job seekers. | Apply threshold changes quickly and cheaply without changing score history. | [local reliability QA](local-reliability-qa.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-026 | 2026-05-27 | The API must expose structured per-job and bulk actions for current-version rescore and current-version re-tailor, returning accepted command state and run identifiers. | The React app and local automation users. | Provide safe product controls for deliberate recomputation instead of shell-only instructions. | [local TypeScript API](local-ts-api.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-027 | 2026-05-27 | SSE invalidation must cover preparation work-item lifecycle events, scoring policy changes, tailoring policy changes, score-threshold eligibility changes, re-tailor requests, and artifact suppression. | Job seekers monitoring active work. | Keep lists, detail workspaces, dashboards, and artifact views accurate during background preparation work. | [frontend target](architecture/frontend/index.md), [pipeline architecture](architecture/pipeline/index.md) |
| TR-028 | 2026-05-27 | QA coverage must include integrated Discovery preparation, policy-version roll-forward and rollback, threshold lowering and raising, soft artifact suppression, and per-job plus bulk rescore/re-tailor product flows. | Maintainers and job seekers. | Prevent regressions in the workflows most likely to waste LLM spend or show stale materials. | [local reliability QA](local-reliability-qa.md) |

### Materials Rendering

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-030 | 2026-06-24 | The resume PDF renderer must be HTML/CSS printed by Playwright, while still producing the existing `resume_pdf` artifact type with `RenderFormat.HTML_PDF`; historical `latex_pdf` rows are read/migration compatibility only and must not invoke a TeX engine for new renders. | Maintainers and local users. | Keep downstream apply readiness and artifact compatibility while using the HTML/CSS renderer. | [architecture](architecture/index.md), [DDD target](architecture/domain-model/index.md) |
| TR-031 | 2026-06-24 | HTML-rendered resume artifacts must persist a generated sibling HTML file and generation-time layout boxes; the API must expose `/preview.html` only for supported `html_pdf` resume artifacts and project layout boxes into Apply Review materials preview data. | The API, web app, and maintainers. | Keep the final PDF, review surface, and line-level audit anchors tied to one generated render source. | [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |
| TR-032 | 2026-06-26 | Resume template changes must be applied lazily through an ensure-current-materials path when Apply Review or artifact open needs current resume materials; successful refresh writes a new render-only generation and unsuccessful refresh preserves the last accepted artifacts. | The API, web app, and maintainers. | Avoid eager regeneration work while keeping review/open flows current and auditable. | [local TypeScript API](local-ts-api.md), [architecture](architecture/index.md), [local reliability QA](local-reliability-qa.md) |

### Security And Observability

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-013 | 2026-05-22 | Credentials must use a secret port such as macOS Keychain or explicit environment variables, never SQLite, snapshots, logs, traces, or artifacts. | Job seekers. | Protect accounts used during local search and application workflows. | [architecture](architecture/index.md) |
| TR-014 | 2026-05-22 | Observability must cover LLM, workflow, and JSON-RPC activity without exporting raw private content such as resumes, generated materials, job text, credentials, local paths, logs, or databases. | Maintainers and local operators. | Debug behavior and cost while preserving user privacy. | [architecture](architecture/index.md) |
| TR-015 | 2026-05-22 | Langfuse and telemetry export must be configurable and disableable, including a local disable path for private runs. | Job seekers and maintainers. | Support local privacy choices and development diagnostics. | [architecture](architecture/index.md) |
| BR-055 | 2026-07-04 | A release privacy gate must scan tracked and untracked files, plus built distribution archives, for private-profile needles, secret assignments, forbidden filenames, and browser-profile artifacts. It runs automatically on every push to `main` and release publication; public pull requests intentionally do not receive heavyweight CI automatically, so a maintainer must run the scanner locally or through the manual workflow after review and before merge. | Maintainers and future users. | Keep personal data and secrets out of the public repository without exposing unreviewed contributor code to privileged or heavyweight automation. | [developer security](developer/security.md) |
| TR-046 | 2026-09-02 | The live Discovery browser bridge must bind bounded, idempotent tasks to the exact workflow/run execution; use loopback token authentication, public-URL validation, leases, cancellation, timeout, and payload caps; keep task bodies/results in process memory only; reject worker-supplied browser cookies/user-agent/origin headers; and expose a fresh heartbeat separately from token presence. Chrome must own its current cookies, session, proxy, and user agent, with the returned browser identity used for robots evaluation. | Maintainers and local users. | Prevent pairing from being mistaken for readiness, prevent stale or cross-execution browser results, and reuse live browser state without copying its secrets into worker configuration or durable bridge storage. | [security](user/security.md), [local TypeScript API](local-ts-api.md), [local reliability QA](local-reliability-qa.md) |

### Verification And Quality

| ID | Added | Requirement | For whom | Why | Related docs |
| --- | --- | --- | --- | --- | --- |
| TR-016 | 2026-05-22 | Missing frontend event handlers or stage badges must be caught by tests that cover the domain event union and stage-state kinds. | Maintainers. | Keep new domain states from silently rendering incorrectly. | [frontend target](architecture/frontend/index.md), [local reliability QA](local-reliability-qa.md) |
| TR-017 | 2026-05-22 | Scoring changes must be covered by evaluation fixtures and metrics for parsing, deterministic policy resolution, blockers, ranking, corrections, anchor consistency, stale-score precision, and reproducibility. | Maintainers. | Prevent prompt, policy, or parser changes from degrading decisions unnoticed. | [local reliability QA](local-reliability-qa.md) |
| TR-018 | 2026-05-22 | Meaningful user-facing behavior changes must include product-path verification, not only unit tests. | Job seekers and maintainers. | Catch failures that only appear in real local workflows. | [local reliability QA](local-reliability-qa.md), [frontend target](architecture/frontend/index.md) |
| TR-019 | 2026-05-22 | High-risk local workflows must stay covered by the reliability matrix, including dry-run apply, apply timeout, retry, blocking, artifact opening, profile save/discard, source filtering, stale-score correction, and global list sorting. | Job seekers. | Prevent regressions in workflows that can waste time or corrupt local state. | [local reliability QA](local-reliability-qa.md) |
| TR-020 | 2026-05-22 | Frontend accessibility defects found in production paths must be fixed or recorded as explicit backlog deferrals with production references. | Job seekers using assistive technology. | Keep the local UI usable and prevent silent a11y regressions. | [local reliability QA](local-reliability-qa.md), [backlog](backlog.md) |
| TR-021 | 2026-05-22 | Root scripts must expose the documented web test, type-level test, coverage, watch, Playwright, and extension verification commands: `web:test`, `web:test:watch`, `web:test:coverage`, `web:test-d`, `web:e2e`, `web:e2e:headed`, `extension:check`, `extension:test`, `extension:build`, and `extension:e2e`. | Contributors. | Make verification discoverable and consistent from the repo root. | [package scripts](../package.json) |
