---
phase: 07-shared-primitive-token-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/shared/ui/filterable-data-grid.tsx
  - apps/web/src/shared/ui/filterable-data-grid.test.tsx
  - apps/web/src/shared/ui/filterable-data-grid.stories.tsx
  - apps/web/src/shared/ui/table-pager.tsx
  - apps/web/src/shared/ui/table-pager.test.tsx
  - apps/web/src/shared/ui/table-pager.stories.tsx
  - apps/web/src/styles/globals.css
autonomous: true
requirements:
  - PRIM-01
  - PRIM-03
  - PRIM-04
user_setup: []
must_haves:
  truths:
    - "FilterableDataGrid preserves filtering, sorting, pagination, row activation, active filter chips, and dialog filter behavior."
    - "TablePager preserves native previous/next disabled behavior and page-size select behavior."
    - "Dense table/grid focus states are visible and stable across compact, regular, and comfy row-height seams."
  artifacts:
    - path: "apps/web/src/shared/ui/filterable-data-grid.stories.tsx"
      provides: "FilterableDataGrid populated, loading, empty, filtered, paginated, and activatable-row states"
    - path: "apps/web/src/shared/ui/table-pager.test.tsx"
      provides: "TablePager disabled edge and page-size behavior tests"
    - path: "apps/web/src/shared/ui/filterable-data-grid.test.tsx"
      provides: "Extended high-risk grid behavior coverage"
  key_links:
    - from: "apps/web/src/shared/ui/filterable-data-grid.tsx"
      to: "apps/web/src/shared/ui/table-pager.tsx"
      via: "pagination footer composition"
      pattern: "TablePager"
    - from: "apps/web/src/styles/globals.css"
      to: "apps/web/src/shared/ui/filterable-data-grid.tsx"
      via: "data-grid focus/density class contract"
      pattern: "filterable-data-grid-table.*focus-visible"
---

<objective>
Add focused behavior and state coverage for the dense table/data-grid/pager primitives.

Purpose: D-06 identifies `filterable-data-grid`, `data-table`, and `table-pager` as high-risk because they combine density, custom keyboard behavior, filters, and pagination. This plan proves the grid/pager surface without route, API, query, SSE, or worker changes.
Output: Extended RTL tests, a new FilterableDataGrid story surface, pager tests/states, and any narrow semantic focus styling needed to satisfy D-10 and D-11.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-shared-primitive-token-migration/07-CONTEXT.md
@.planning/phases/07-shared-primitive-token-migration/07-RESEARCH.md
@.planning/phases/07-shared-primitive-token-migration/07-PATTERNS.md
@.planning/phases/07-shared-primitive-token-migration/07-UI-SPEC.md
@.planning/phases/07-shared-primitive-token-migration/07-VALIDATION.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-06-SUMMARY.md
@docs/frontend-target.md
@docs/local-reliability-qa.md
@apps/web/src/shared/ui/filterable-data-grid.tsx
@apps/web/src/shared/ui/filterable-data-grid.test.tsx
@apps/web/src/shared/ui/table-pager.tsx
@apps/web/src/shared/ui/table-pager.stories.tsx
@apps/web/src/styles/globals.css
</context>

## Artifacts This Phase Produces

- `apps/web/src/shared/ui/data-table.test.tsx` - new DataTable semantic and keyboard regression tests.
- `apps/web/src/shared/ui/toast.a11y.test.tsx` - new Toast/Toaster accessibility regression tests.
- `apps/web/src/shared/ui/table-pager.test.tsx` - new TablePager behavior regression tests.
- `apps/web/src/shared/ui/filterable-data-grid.stories.tsx` - new FilterableDataGrid state stories.
- `apps/web/src/shared/lib/job-description-blocks.ts` - new shared pure helper exporting `descriptionBlocks`.
- `apps/web/src/shared/lib/job-description-blocks.test.ts` - new helper regression tests.
- `.planning/phases/07-shared-primitive-token-migration/07-PRIMITIVE-AUDIT.md` - phase audit evidence from static scans and verification commands.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FilterableDataGrid behavior tests for activation, focus, and filters</name>
  <files>apps/web/src/shared/ui/filterable-data-grid.tsx, apps/web/src/shared/ui/filterable-data-grid.test.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    @apps/web/src/shared/ui/filterable-data-grid.tsx
    @apps/web/src/shared/ui/filterable-data-grid.test.tsx
    @apps/web/src/styles/globals.css
    @.planning/phases/07-shared-primitive-token-migration/07-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: row activation fires for click, Enter, and Space and does not fire for unrelated keys.
    - Test 2: filter dialog controls are reachable by role/name, active filter chips clear the exact column, and focus-visible controls expose a visible indicator.
    - Test 3: page rows update when filtering, sorting, and page-size changes combine.
  </behavior>
  <action>Extend `filterable-data-grid.test.tsx` before source edits per D-08 and D-10. Use synthetic rows only per D-09. Preserve all public props, local/controlled sort/filter modes, pagination callbacks, `onPageRowsChange`, `rowAriaSelected`, `rowClassName`, table markup, and Dialog/Input/TablePager composition per D-02. If the new tests expose insufficient visible focus for row activation or filter buttons, make the narrowest change in `filterable-data-grid.tsx` or the existing data-grid section of `globals.css` using standard semantic tokens such as `--ring`, `--background`, `--foreground`, `--border`, or shadcn utilities already used by the primitive. Keep density on `.app-shell[data-density]` and `--jh-row-height`; do not replace it with container style queries per D-11.</action>
  <acceptance_criteria>
    - Existing filtering/sorting test remains intact and passes.
    - New tests prove click, Enter, Space, filter-chip clearing, and page-row update behavior.
    - Any focus styling change is limited to grid/pager selectors and uses standard semantic token names, not legacy `bg-paper`, `text-ink`, `border-rule`, `ring-info`, or direct legacy variables.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/shared/ui/filterable-data-grid.test.tsx</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>FilterableDataGrid has focused regression coverage for the custom keyboard/filter/pagination behavior most likely to regress during token and story hardening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TablePager tests and preserve native control behavior</name>
  <files>apps/web/src/shared/ui/table-pager.tsx, apps/web/src/shared/ui/table-pager.test.tsx, apps/web/src/shared/ui/table-pager.stories.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    @apps/web/src/shared/ui/table-pager.tsx
    @apps/web/src/shared/ui/table-pager.stories.tsx
    @apps/web/src/styles/globals.css
    @.planning/phases/07-shared-primitive-token-migration/07-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: previous is disabled on page 1, next is disabled on the last page, and neither disabled button calls `onPageChange`.
    - Test 2: enabled previous/next call `onPageChange` with page minus/plus one.
    - Test 3: changing `Page size` calls `onPageSizeChange` with the numeric option value.
  </behavior>
  <action>Create `apps/web/src/shared/ui/table-pager.test.tsx` using Testing Library and `userEvent`. Keep native `<button disabled>` and `<select aria-label="Page size">` behavior per D-02 and D-10. If source styling changes are necessary, keep labels `previous`, `next`, and `Page size`, keep dimensions stable, and use semantic utilities or the existing `.pager`/`.tab` semantic token styling. Do not add app-specific status colors or domain imports; this remains a generic shared primitive per D-04.</action>
  <acceptance_criteria>
    - `table-pager.test.tsx` exists and covers disabled edges, enabled navigation, and page-size change.
    - The `Page size` select remains a native select with an accessible name.
    - No new `data-testid` or snapshot-only assertion is used for this behavior.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/shared/ui/table-pager.test.tsx</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>TablePager behavior is covered independently from product views and keeps native disabled/select semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add table/grid/pager Storybook states with synthetic dense data</name>
  <files>apps/web/src/shared/ui/filterable-data-grid.stories.tsx, apps/web/src/shared/ui/table-pager.stories.tsx</files>
  <read_first>
    @apps/web/src/shared/ui/filterable-data-grid.tsx
    @apps/web/src/shared/ui/table-pager.stories.tsx
    @apps/web/src/shared/ui/data-table.stories.tsx
    @.planning/phases/07-shared-primitive-token-migration/07-UI-SPEC.md
  </read_first>
  <action>Add `filterable-data-grid.stories.tsx` with synthetic rows and stories for populated, loading, empty, filtered/no-match, paginated, and activatable rows per D-07 and D-09. Keep story rows generic and non-sensitive; do not use real job URLs or generated material paths. Extend `table-pager.stories.tsx` only where states are missing, such as disabled first/last/single-page and a compact-width example. Do not silence Storybook a11y. Do not snapshot Radix/shadcn internals per D-08.</action>
  <acceptance_criteria>
    - `filterable-data-grid.stories.tsx` exists and exercises loading, empty, populated, filtered, paginated, and activatable-row states.
    - `table-pager.stories.tsx` still exposes first, middle, last, and single-page states, with no sensitive fixture content.
    - Story exports render without importing contexts, views, API clients, query hooks, local storage, EventSource, or route modules.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:storybook:build</automated>
    <automated>corepack pnpm web:storybook:test</automated>
  </verify>
  <done>Dense table/grid/pager states are reviewable in Storybook and covered by automated story/a11y execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic grid fixtures -> Storybook/Vitest | Dense table fixture content must stay artificial and non-sensitive. |
| Storybook/Vitest -> local browser/runtime | Tests can drive controls but must not trigger product workflows or workers. |
| shared/ui -> app/domain modules | Grid and pager primitives must remain generic shared UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-I | Information Disclosure | Grid/pager stories and tests | mitigate | Use synthetic companies, providers, and counters only; no profile, resume, PDF, DB, log, job URL, API key, OAuth, or generated material data. |
| T-07-02-E | Elevation of Privilege | QA execution | mitigate | Run Vitest and Storybook only; do not run auto-apply, browser submission, mailbox scans, real material generation, destructive profile/database actions, or worker-backed jobs. |
| T-07-02-B | Tampering | `shared/ui` imports | mitigate | Keep story/test imports inside shared/ui and test utilities; Plan 07-05 runs a blocking boundary scan. |
| T-07-02-SC | Tampering | package installs | mitigate | No package installs; use existing Vitest, Testing Library, Storybook, and Playwright tooling. |
</threat_model>

<verification>
Run task-level commands plus the corrected shared/ui token scanner from `07-RESEARCH.md` and `git diff --check`.
</verification>

<success_criteria>
PRIM-01, PRIM-03, and PRIM-04 are covered for dense table/data-grid/pager primitives with semantic focus styling, stable dimensions, behavior tests, and per-state Storybook coverage.
</success_criteria>

<output>
Create `.planning/phases/07-shared-primitive-token-migration/07-02-SUMMARY.md` when done.
</output>
