---
phase: 12-folded-cleanup-verification-baseline
plan: 12-02
status: pending
---

# 12-02 Documentation And Baseline Verification

## Goal

Normalize current-state docs/maps and record the Phase 12 verification baseline.

## Tasks

- Update current-state codebase maps that still describe `apps/web/tailwind.config.ts` as active.
- Update current-state frontend documentation that still claims visible `lucide-react` imports remain compatible.
- Leave historical implemented-plan references intact when they describe the system state at the time they were written.
- Run stale Tailwind config and legacy token scans.
- Update requirements, roadmap, state, and phase summary/verification after checks pass.

## Threat Model

- **Sensitive data exposure:** Docs must not include local profile, resume, generated artifact, browser profile, log, database, API key, or OAuth data.
- **Integrity risk:** Do not rewrite historical implementation records as current state.
- **Scope risk:** Documentation edits must not reopen visual-system migration or change product behavior.

## Verification

- `rg "apps/web/tailwind.config.ts|tailwind.config" README.md docs AGENTS.md .planning apps/web`
- `rg "var\\(--bg|var\\(--paper|var\\(--ink|bg-paper|text-ink|border-rule|ring-info|--danger|--warn|--ok|--info" apps/web/src apps/web/package.json apps/web/components.json`
- `git diff --check`

