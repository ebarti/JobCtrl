---
phase: 06-token-foundation-shadcn-preset-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [TOKEN-03, TOKEN-04]
must_haves:
  truths:
    - "Supply-chain review for D-07 dependencies is complete before any install or shadcn@latest dlx execution."
    - "SUS packages from T-06-01 are approved or the phase stops before dependency changes."
  artifacts:
    - path: ".planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md"
      provides: "Package Legitimacy Audit with SUS package list"
  key_links:
    - from: "06-01 checkpoint"
      to: "06-02 dependency install"
      via: "depends_on"
      pattern: "06-02 depends_on includes 06-01"
---

<objective>
Gate Phase 6 package installation and shadcn CLI use through the required human package legitimacy checkpoint.

Purpose: T-06-01 identifies `shadcn`, `@fontsource-variable/geist`, and `@types/node` as suspicious because their latest releases are recent. D-07 requires installing them, so execution must stop for human verification before any install or `shadcn@latest` command runs.
Output: Human approval or a blocked phase before dependency/config changes.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
</context>

## Artifacts this phase produces

- Blocking package legitimacy approval for `shadcn`, `@fontsource-variable/geist`, and `@types/node`.
- A dependency gate that lets Plan 06-02 install `shadcn@4.11.0`, `@fontsource-variable/geist@5.2.9`, and `@types/node@25.9.2` only after approval.
- No source-code edits in this plan.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve SUS package identities before install</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
  </read_first>
  <files>None</files>
  <action>
    Per D-07 and T-06-01, stop before installing dependencies or running `corepack pnpm dlx shadcn@latest`. Ask the human to verify current npm package identity for `shadcn`, `@fontsource-variable/geist`, and `@types/node`: npm package page, source repository, latest version, publish date, maintainers, and absence of unreviewed `postinstall` behavior. Expected research-time identities are `shadcn@4.11.0` from `github.com/shadcn-ui/ui`, `@fontsource-variable/geist@5.2.9` from `github.com/fontsource/font-files`, and `@types/node@25.9.2` from `github.com/DefinitelyTyped/DefinitelyTyped`. If current latest metadata differs materially from research, block and report the difference instead of installing.
  </action>
  <acceptance_criteria>
    Human explicitly approves all three SUS package identities before Plan 06-02 runs. If any package is rejected or metadata materially conflicts with research, execution records the blocker and no dependency files change.
  </acceptance_criteria>
  <verify>
    <automated>node /Users/eloibarti/.codex/gsd-core/bin/gsd-tools.cjs query init.plan-phase 06</automated>
    <human-check>Human confirms package identity approval for `shadcn`, `@fontsource-variable/geist`, and `@types/node`.</human-check>
  </verify>
  <done>Approval is recorded in the plan summary, or the phase is blocked before dependency installation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local dependency tree | Package metadata and tarballs become executable/build-time inputs. |
| human review to automated install | Human approval controls whether Plan 06-02 may run install commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `shadcn`, `@fontsource-variable/geist`, `@types/node` package install | mitigate | Blocking human package identity checkpoint before install or dlx use. |
| T-06-SC | Tampering | npm installs | mitigate | Use the RESEARCH.md Package Legitimacy Audit; no install if SUS package identity is not approved. |
| T-06-02 | Information Disclosure | package review evidence | accept | No sensitive app data is needed for package review; do not paste local profile, artifact, log, or DB contents. |
</threat_model>

<verification>
This plan is complete only when the checkpoint is approved or explicitly blocked. No code, lockfile, or config file should change in this plan.
</verification>

<success_criteria>
Plan 06-02 can safely install the required dependencies because the SUS packages named in T-06-01 were reviewed and approved.
</success_criteria>

<output>
Create `.planning/phases/06-token-foundation-shadcn-preset-contract/06-01-SUMMARY.md` when done.
</output>
