---
phase: 06-token-foundation-shadcn-preset-contract
plan: "06"
type: execute
wave: 5
depends_on: ["06-04", "06-05"]
files_modified:
  - apps/web/e2e/tests/token-foundation.spec.ts
  - docs/frontend-target.md
  - docs/local-reliability-qa.md
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-04, TOKEN-05, TOKEN-06]
autonomous: true
must_haves:
  truths:
    - "D-16, D-17, D-18, D-19, and D-20 are proven by build, shadcn info, token grep, browser computed-token smoke, and safe QA boundaries."
    - "The app shell computes light/dark shadcn tokens and density row heights through existing theme/density controls."
    - "Docs record the CSS-first shadcn token contract and the Phase 6 QA gate."
  artifacts:
    - path: "apps/web/e2e/tests/token-foundation.spec.ts"
      provides: "Browser computed-token smoke for light/dark/density/focus/control surfaces"
    - path: "docs/frontend-target.md"
      provides: "Updated frontend styling target for CSS-first shadcn tokens"
    - path: "docs/local-reliability-qa.md"
      provides: "Token foundation QA command/checklist entry"
  key_links:
    - from: "apps/web/e2e/tests/token-foundation.spec.ts"
      to: "apps/web/src/shared/layout/Topbar.tsx"
      via: "role selectors for theme and row density"
      pattern: "Row density|Switch to"
    - from: "docs/frontend-target.md"
      to: "apps/web/src/styles/globals.css"
      via: "CSS-first token documentation"
      pattern: "@theme inline"
---

<objective>
Add the browser computed-token smoke, update owning docs narrowly, and run the complete Phase 6 proof gate.

Purpose: Compile-only validation is insufficient for this phase. This plan proves the app shell, theme selector, density selector, focus state, native control color-scheme, shadcn CLI contract, generated utilities, and grep-clean exit state using synthetic/seeded data only.
Output: Playwright token-foundation smoke spec, narrow docs updates, complete validation evidence, and final source coverage audit.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
@docs/frontend-target.md
@docs/local-reliability-qa.md
@docs/local-ts-api.md
@docs/architecture.md
@docs/decisions.md
@apps/web/e2e/playwright.config.ts
@apps/web/e2e/tests/dashboard.spec.ts
@apps/web/src/shared/layout/Topbar.tsx
@apps/web/src/shared/layout/ThemeToggle.tsx
@apps/web/src/shared/layout/AppShell.tsx
</context>

## Artifacts this phase produces

- Browser smoke test file: `apps/web/e2e/tests/token-foundation.spec.ts`.
- Test assertions for computed `--background`, `--foreground`, `--primary`, `--ring`, `--sidebar`, `color-scheme`, topbar/nav color/border values, focused theme-control focus indicator, row-density variable values `32px`, `40px`, `48px`, and topbar native density select styling.
- Docs update in `docs/frontend-target.md`: Tailwind CSS 4 now uses CSS-first `@theme inline`, `shadcn/tailwind.css`, Fontsource imports, `[data-theme="dark"]`, and app-shell-scoped density tokens.
- Docs update in `docs/local-reliability-qa.md`: Phase 6 token foundation QA gate and safe browser proof requirements.
- Final proof commands: `corepack pnpm web:check`, `corepack pnpm web:build`, `corepack pnpm dlx shadcn@latest info -c apps/web`, phase-wide legacy grep, generated CSS utility grep, browser smoke, and `git diff --check`.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 6 | Standard shadcn semantic token foundation with legacy token names removed | 01-06 | COVERED | Plans gate packages, configure preset, implement tokens, clean utilities, and prove browser/build exit. |
| REQ | TOKEN-01 | Light/dark shadcn variables for core, chart, sidebar, fonts, radius | 03,06 | COVERED | CSS contract and token test. |
| REQ | TOKEN-02 | Tailwind 4 generates semantic utilities via `@theme inline`; legacy utilities removed | 03,04,05,06 | COVERED | CSS mappings, mechanical cleanup, generated CSS proof. |
| REQ | TOKEN-03 | Decoded preset represented: radix-luma, neutral, sky, amber, medium radius, Geist, JetBrains Mono, Tabler, default-translucent menu, subtle accent | 01,02,03,06 | COVERED | Package gate, dependencies, components.json, CSS values, docs. |
| REQ | TOKEN-04 | shadcn CLI validation with aliases under `apps/web/src/shared/ui` | 01,02,06 | COVERED | Package gate, Vite and TS aliases, final `shadcn info`. |
| REQ | TOKEN-05 | `[data-theme="dark"]` and app-shell density behavior remain compatible | 03,06 | COVERED | Theme/density CSS plus Playwright computed smoke. |
| REQ | TOKEN-06 | Legacy aliases/utilities absent; no compile bridge remains | 03,04,05,06 | COVERED | Config bridge deletion, mechanical cleanup, phase-wide grep. |
| RESEARCH | T-06-01 | Supply-chain tampering from suspicious latest releases | 01,02 | COVERED | Blocking checkpoint and named installs. |
| RESEARCH | T-06-02 | Sensitive data exposure in screenshots/browser proof | 06 | COVERED | E2E seeded data and no user-affecting automation. |
| RESEARCH | T-06-03 | Legacy token bridge survives | 03,04,05,06 | COVERED | Delete bridge and grep exit gate. |
| RESEARCH | T-06-04 | Visual polish breaks a11y or status meaning | 03,04,05,06 | COVERED | Focus/ring tokens, status extensions, browser smoke. |
| CONTEXT | D-01 | Preset active immediately | 03 | COVERED | CSS values activated in token contract. |
| CONTEXT | D-02 | Clean-slate migration | 03,04,05 | COVERED | Legacy token contract removed mechanically. |
| CONTEXT | D-03 | Legacy aliases/utilities do not remain | 03,04,05,06 | COVERED | Regex/test/grep gates. |
| CONTEXT | D-04 | Any compile bridge removed before completion | 03,06 | COVERED | Tailwind config deletion and final grep. |
| CONTEXT | D-05 | CSS-first `@theme inline`, no old config bridge | 03 | COVERED | `@theme inline` and deleted config. |
| CONTEXT | D-06 | Keep `shadcn` dependency and `shadcn/tailwind.css` | 02,03 | COVERED | Installed dependency and import. |
| CONTEXT | D-07 | Install preset dependencies | 01,02 | COVERED | Checkpoint and install task. |
| CONTEXT | D-08 | Theme-only shadcn CLI apply | 02 | COVERED | Exact command. |
| CONTEXT | D-09 | No uncontrolled full apply | 02,04,05 | COVERED | Reject UI rewrites; mechanical edits only. |
| CONTEXT | D-10 | `components.json` final target | 02,03 | COVERED | Style, icon, css variables, aliases, blank config. |
| CONTEXT | D-11 | Remove legacy aliases now | 03,04,05,06 | COVERED | Cleanup plans and grep. |
| CONTEXT | D-12 | Broad styling edits allowed only mechanically | 04,05 | COVERED | File-scoped class replacements. |
| CONTEXT | D-13 | Rename app status extensions | 03 | COVERED | `--success`, `--warning`, `--status-info` semantic tokens. |
| CONTEXT | D-14 | Do not flatten statuses into chart tokens | 03,05 | COVERED | Status extensions preserved; chart tokens data-only. |
| CONTEXT | D-15 | Delete Tailwind config if CSS-first works | 03 | COVERED | Planner chooses deletion. |
| CONTEXT | D-16 | Build plus browser proof | 06 | COVERED | Full proof gate. |
| CONTEXT | D-17 | Type/build/shadcn/grep/diff validation | 06 | COVERED | Final proof commands. |
| CONTEXT | D-18 | App-shell browser proof | 06 | COVERED | Token-foundation Playwright spec. |
| CONTEXT | D-19 | Full stack acceptable for browser proof | 06 | COVERED | E2E webServer uses local API and web. |
| CONTEXT | D-20 | Do not trigger user-affecting automation | 06 | COVERED | Seeded E2E and no apply/material/worker actions. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser computed-token smoke</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
    apps/web/e2e/playwright.config.ts
    apps/web/e2e/tests/dashboard.spec.ts
    apps/web/src/shared/layout/Topbar.tsx
    apps/web/src/shared/layout/ThemeToggle.tsx
    apps/web/src/shared/layout/AppShell.tsx
  </read_first>
  <files>apps/web/e2e/tests/token-foundation.spec.ts</files>
  <behavior>
    - `/dashboard` renders with non-empty computed shadcn tokens on `document.documentElement`.
    - Theme toggle changes `data-theme` and `color-scheme` from light to dark.
    - Row density select changes app-shell `data-density` and computed `--jh-row-height` values to 32px, 40px, and 48px.
    - Topbar/nav/control surfaces have non-transparent computed background, border, and focus indicators.
    - The smoke uses the existing E2E seeded temp app directory and does not trigger apply, browser submission, mailbox scanning, material generation, destructive actions, or worker-backed jobs.
  </behavior>
  <action>
    Create `apps/web/e2e/tests/token-foundation.spec.ts` following the existing Playwright style. Visit `/dashboard`, inspect `document.documentElement` computed values for `--background`, `--foreground`, `--primary`, `--ring`, `--sidebar`, and `color-scheme`. Use the existing topbar controls by role: `button` with name matching `Switch to dark theme` and `combobox` with name `Row density`. Select `compact`, `regular`, and `comfy`, then assert `.app-shell` has matching `data-density` and computed `--jh-row-height` values `32px`, `40px`, and `48px`. Focus the theme button with keyboard and assert a visible focus indicator through `outlineStyle`, `outlineWidth`, or `boxShadow`. Inspect `.topbar`, `.nav a`, and the native row-density select for non-empty computed background, border, foreground, and color-scheme values. Navigate to `/jobs` only to verify a dense list route renders under the same shell; do not click apply, generate, delete, hide, submit, mailbox, worker, or destructive controls per D-20 and T-06-02.
  </action>
  <acceptance_criteria>
    The Playwright smoke proves app-shell light/dark tokens, density row heights, focus visibility, and native control color-scheme with seeded E2E data only. It is deterministic under the existing `apps/web/e2e/playwright.config.ts` temp DB/app-dir setup.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web e2e -- tests/token-foundation.spec.ts</automated>
  </verify>
  <done>Browser computed-token proof exists and avoids all user-affecting automation.</done>
</task>

<task type="auto">
  <name>Task 2: Update owning frontend and QA docs narrowly</name>
  <read_first>
    AGENTS.md
    docs/frontend-target.md
    docs/local-reliability-qa.md
    docs/local-ts-api.md
    docs/architecture.md
    docs/decisions.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
  </read_first>
  <files>docs/frontend-target.md, docs/local-reliability-qa.md</files>
  <action>
    Update `docs/frontend-target.md` narrowly where it still describes `tailwind.config.ts` as the design-token bridge. The new target state is Tailwind CSS 4 with CSS-first `@theme inline`, `shadcn/tailwind.css`, `tw-animate-css`, Fontsource Geist and JetBrains Mono imports, semantic tokens in `tokens.css`, `[data-theme="dark"]`, and app-shell-scoped density variables. Update `docs/local-reliability-qa.md` with a Phase 6 token foundation QA entry listing the final proof commands from this plan, synthetic/seeded browser proof, and the no auto-apply/material-generation/destructive-action rule. Do not edit `docs/local-ts-api.md` because Phase 6 does not change API behavior or web command names.
  </action>
  <acceptance_criteria>
    Docs accurately describe the final token/config/QA contract without adding broad migration narrative or changing API/SSE/worker documentation. The docs mention the required final proof commands and synthetic-data safety boundary.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@theme inline|shadcn/tailwind.css|token foundation|data-theme|density" docs/frontend-target.md docs/local-reliability-qa.md</automated>
  </verify>
  <done>Owning docs match the implemented Phase 6 frontend token foundation and QA gate.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 6 proof gate</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
    apps/web/src/styles/token-contract.test.ts
    apps/web/e2e/tests/token-foundation.spec.ts
    apps/web/components.json
    apps/web/package.json
  </read_first>
  <files>None</files>
  <action>
    Run the required final proof from D-16, D-17, and the security constraints. Use `corepack pnpm --filter @jobhunter/web test -- src/styles/token-contract.test.ts`, `corepack pnpm web:check`, `corepack pnpm web:build`, `corepack pnpm dlx shadcn@latest info -c apps/web`, generated CSS utility proof, phase-wide legacy grep, `corepack pnpm --filter @jobhunter/web e2e -- tests/token-foundation.spec.ts`, and `git diff --check`. For generated CSS utility proof, inspect `dist/web/assets/*.css` with a Node one-liner and require `.bg-background`, `.text-foreground`, `.bg-card`, `.border-border`, `.ring-ring`, `.bg-primary`, `.text-primary-foreground`, `.bg-popover`, and `.text-popover-foreground`. For the legacy grep, use PCRE negative lookahead so `text-muted-foreground` and other standard classes do not false-positive, and scan `apps/web/src`, `apps/web/.storybook`, and `apps/web/components.json`. If the grep hits comments or tests, remove or construct those strings from fragments rather than excluding production files. Do not run real auto-apply, browser submission, mailbox scanning, material generation, destructive profile/database actions, or worker-backed jobs.
  </action>
  <acceptance_criteria>
    All required commands pass. `shadcn info` validates aliases and config. `apps/web/tailwind.config.ts` is absent. Legacy token grep returns no hits in scanned app/story/config files. Generated CSS contains required semantic utility selectors. Browser smoke passes with seeded data. `git diff --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test -- src/styles/token-contract.test.ts</automated>
    <automated>corepack pnpm web:check</automated>
    <automated>corepack pnpm web:build</automated>
    <automated>corepack pnpm dlx shadcn@latest info -c apps/web</automated>
    <automated>node -e 'const fs=require("node:fs"); const path=require("node:path"); const dir="dist/web/assets"; const css=fs.readdirSync(dir).filter((f)=>f.endsWith(".css")).map((f)=>fs.readFileSync(path.join(dir,f),"utf8")).join("\\n"); for (const s of [".bg-background",".text-foreground",".bg-card",".border-border",".ring-ring",".bg-primary",".text-primary-foreground",".bg-popover",".text-popover-foreground"]) { if (!css.includes(s)) throw new Error(`Missing generated utility ${s}`); }'</automated>
    <automated>! rg -n -P -- '--(?:bg|paper|paper-2|ink|rule|rule-2|info|danger|warn|ok|font|mono|row)\\b|\\b(?:bg|text|border|ring|ring-offset|focus:bg|aria-selected:bg|data-\\[state=open\\]:bg|data-\\[state=active\\]:bg)-(?:bg|paper|paper-2|ink|rule|rule-2|danger|warn|ok|info|soft|muted)(?!-)' apps/web/src apps/web/.storybook apps/web/components.json</automated>
    <automated>corepack pnpm --filter @jobhunter/web e2e -- tests/token-foundation.spec.ts</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Phase 6 has complete build, CLI, grep, generated CSS, browser, safety, and diff proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser smoke to local app data | Playwright opens local app routes and could expose local data if not seeded. |
| docs to future implementation | Docs become the source for future frontend token and QA expectations. |
| final proof to phase completion | Passing final commands gates whether Phase 6 is complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | browser computed-token smoke | mitigate | Use existing E2E temp app directory and seeded SQLite fixture; no screenshots with sensitive local data; no user-affecting automation. |
| T-06-03 | Tampering | legacy bridge exit state | mitigate | Final phase-wide grep and `test ! -e apps/web/tailwind.config.ts` through earlier plan proof. |
| T-06-04 | Denial of Use | app shell focus, density, native controls | mitigate | Playwright computed-token smoke checks focus indicator, color-scheme, and 32/40/48 density values. |
| T-06-01 | Tampering | shadcn CLI proof | mitigate | Final `shadcn@latest info` validates config after package checkpoint and install plans. |
</threat_model>

<verification>
Run the full proof listed in Task 3. Record exact command results in `06-06-SUMMARY.md`, including any skipped command with concrete reason.
</verification>

<success_criteria>
Phase 6 is complete when the app has the standard shadcn semantic token foundation, decoded preset/config contract, generated utility proof, preserved theme/density behavior, grep-clean legacy token exit state, safe browser proof, narrow docs, and passing diff hygiene.
</success_criteria>

<output>
Create `.planning/phases/06-token-foundation-shadcn-preset-contract/06-06-SUMMARY.md` when done.
</output>
