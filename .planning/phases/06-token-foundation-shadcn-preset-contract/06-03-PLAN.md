---
phase: 06-token-foundation-shadcn-preset-contract
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - apps/web/src/styles/tokens.css
  - apps/web/src/styles/globals.css
  - apps/web/index.html
  - apps/web/tailwind.config.ts
  - apps/web/tsconfig.json
  - apps/web/components.json
  - apps/web/src/styles/token-contract.test.ts
requirements: [TOKEN-01, TOKEN-02, TOKEN-03, TOKEN-05, TOKEN-06]
autonomous: true
must_haves:
  truths:
    - "D-01 and D-02 activate the preset immediately; no parallel old palette remains."
    - "D-03, D-04, D-05, D-11, and D-15 remove the public legacy token contract and Tailwind config bridge by phase exit."
    - "D-13 and D-14 preserve status semantics with semantic extension tokens, not chart-token flattening."
    - "D-18 preserves `[data-theme=\"dark\"]` and app-shell `data-density` behavior with 32/40/48px row heights."
  artifacts:
    - path: "apps/web/src/styles/tokens.css"
      provides: "Light/dark shadcn semantic token variables plus status extensions"
    - path: "apps/web/src/styles/globals.css"
      provides: "Tailwind v4 CSS-first imports, `@theme inline`, base rules, global CSS mechanical token conversion"
    - path: "apps/web/src/styles/token-contract.test.ts"
      provides: "Static token/config contract regression test"
    - path: "apps/web/index.html"
      provides: "Page-level color-scheme metadata"
  key_links:
    - from: "apps/web/src/styles/globals.css"
      to: "apps/web/src/styles/tokens.css"
      via: "@import ./tokens.css"
      pattern: "@import \"./tokens.css\""
    - from: "apps/web/src/styles/globals.css"
      to: "Tailwind v4 utilities"
      via: "@theme inline"
      pattern: "@theme inline"
---

<objective>
Replace the legacy token API with the standard shadcn CSS-variable contract, preserve theme/density seams, delete the Tailwind config bridge, and add a static token contract test.

Purpose: TOKEN-01, TOKEN-02, TOKEN-05, and TOKEN-06 are the foundation for the rest of the milestone. This plan turns the CLI/config baseline from 06-02 into the actual public token contract.
Output: Standard semantic tokens, `@theme inline` utility mappings, clean status extension names, density row-height variables, deleted legacy Tailwind bridge, and a regression test.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
@apps/web/src/styles/tokens.css
@apps/web/src/styles/globals.css
@apps/web/index.html
@apps/web/tailwind.config.ts
@apps/web/tsconfig.json
@apps/web/components.json
</context>

## Artifacts this phase produces

- `@theme inline` mappings for shadcn tokens: `--color-background`, `--color-foreground`, `--color-card`, `--color-card-foreground`, `--color-popover`, `--color-popover-foreground`, `--color-primary`, `--color-primary-foreground`, `--color-secondary`, `--color-secondary-foreground`, `--color-muted`, `--color-muted-foreground`, `--color-accent`, `--color-accent-foreground`, `--color-destructive`, `--color-border`, `--color-input`, `--color-ring`, `--color-chart-1` through `--color-chart-5`, `--color-sidebar`, `--color-sidebar-foreground`, `--color-sidebar-primary`, `--color-sidebar-primary-foreground`, `--color-sidebar-accent`, `--color-sidebar-accent-foreground`, `--color-sidebar-border`, `--color-sidebar-ring`, `--font-sans`, `--font-heading`, `--font-mono`, `--radius-sm`, `--radius-md`, `--radius-lg`, and `--radius-xl`.
- CSS token values for the preset core: light `--background: oklch(1 0 0)`, dark `--background: oklch(0.145 0 0)`, light `--foreground: oklch(0.145 0 0)`, dark `--foreground: oklch(0.985 0 0)`, light `--card: oklch(1 0 0)`, dark `--card: oklch(0.205 0 0)`, light `--sidebar: oklch(0.985 0 0)`, dark `--sidebar: oklch(0.205 0 0)`, light `--primary: oklch(0.5 0.134 242.749)`, dark `--primary: oklch(0.443 0.11 240.79)`, light `--destructive: oklch(0.577 0.245 27.325)`, dark `--destructive: oklch(0.704 0.191 22.216)`, and `--radius: 0.625rem`.
- Status extension tokens under clean names: `--success`, `--success-foreground`, `--success-muted`, `--warning`, `--warning-foreground`, `--warning-muted`, `--status-info`, `--status-info-foreground`, and `--status-info-muted`.
- Density variables: regular `--jh-row-height: 40px`, compact `32px`, comfy `48px`, scoped with `:where(.app-shell[data-density="..."])`.
- Removed file: `apps/web/tailwind.config.ts`.
- New test: `apps/web/src/styles/token-contract.test.ts`.

<tasks>

<task type="auto">
  <name>Task 1: Implement CSS-first semantic tokens and density seams</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
    apps/web/src/styles/tokens.css
    apps/web/src/styles/globals.css
    apps/web/index.html
    apps/web/src/shared/providers/ThemeProvider.tsx
    apps/web/src/shared/layout/AppShell.tsx
  </read_first>
  <files>apps/web/src/styles/tokens.css, apps/web/src/styles/globals.css, apps/web/index.html</files>
  <action>
    Per D-01 and D-02, make the shadcn preset active immediately. In `globals.css`, import `tailwindcss`, `tw-animate-css`, `shadcn/tailwind.css`, `@fontsource-variable/geist`, `@fontsource-variable/jetbrains-mono`, and `./tokens.css`; remove `@config "../../tailwind.config.ts"` per D-05 and D-15. Add `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *));`. Add `@theme inline` mappings for every token listed in this plan's artifacts. In `tokens.css`, replace legacy variables with shadcn semantic variables under `:root` and `:root[data-theme="dark"]`, using the exact core values from the UI-SPEC and preserving chart values generated by the theme-only shadcn output for amber `--chart-1` through `--chart-5`. Use `--status-info` instead of bare `--info` so D-03's legacy `--info` ban and D-13's semantic status-extension intent are both satisfied. Add `color-scheme: light` on `:root` and `color-scheme: dark` on `:root[data-theme="dark"]`; add `<meta name="color-scheme" content="light dark" />` in `index.html`. Replace `var(--row)` consumers with `var(--jh-row-height)` and set regular/compact/comfy row heights to 40/32/48px using app-shell-scoped `:where()` selectors per TOKEN-05.
  </action>
  <acceptance_criteria>
    `globals.css` contains `@theme inline`, `@custom-variant dark`, `shadcn/tailwind.css`, `tw-animate-css`, both Fontsource imports, and no `@config`. `tokens.css` contains all required shadcn semantic tokens in light and dark scopes, semantic status extension tokens, no public legacy aliases from D-03, and the density variables set to 32/40/48px. `index.html` contains `meta name="color-scheme"` while preserving the pre-paint `data-theme` script.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>The CSS token contract is active through CSS-first Tailwind mappings without changing `ThemeProvider`, `AppShell`, routes, API, SSE, worker, or domain behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the legacy Tailwind config bridge</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
    apps/web/tailwind.config.ts
    apps/web/tsconfig.json
    apps/web/components.json
    apps/web/src/styles/globals.css
  </read_first>
  <files>apps/web/tailwind.config.ts, apps/web/tsconfig.json, apps/web/components.json, apps/web/src/styles/globals.css</files>
  <action>
    Per D-05 and D-15, delete `apps/web/tailwind.config.ts` because the plan owns a CSS-first `@theme inline` final state. Remove `"tailwind.config.ts"` from `apps/web/tsconfig.json` includes. Keep `components.json.tailwind.config` as an empty string and `components.json.tailwind.css` as `src/styles/globals.css`. Do not retain old utility colors such as `bg`, `paper`, `ink`, `rule`, `danger`, `warn`, `ok`, or `info` in config, comments, or generated artifacts.
  </action>
  <acceptance_criteria>
    `test -e apps/web/tailwind.config.ts` fails because the file is deleted. `apps/web/tsconfig.json` no longer includes `tailwind.config.ts`. `apps/web/components.json` leaves the Tailwind config field blank and still validates through shadcn CLI.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e apps/web/tailwind.config.ts</automated>
    <automated>corepack pnpm dlx shadcn@latest info -c apps/web</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>The Tailwind config bridge is absent and Tailwind utilities are generated from CSS variables, not legacy config names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the token contract regression test</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-VALIDATION.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
    apps/web/src/contexts/operations/every-event-has-handler.test.ts
    apps/web/vitest.config.ts
    apps/web/src/styles/tokens.css
    apps/web/src/styles/globals.css
    apps/web/components.json
    apps/web/package.json
  </read_first>
  <files>apps/web/src/styles/token-contract.test.ts</files>
  <behavior>
    - Test required shadcn semantic tokens exist in `tokens.css` and are mapped in `globals.css` through `@theme inline`.
    - Test the dark selector is `[data-theme="dark"]` or `:root[data-theme="dark"]`, not a `.dark`-only contract.
    - Test config/package values represent the decoded preset and required packages.
    - Test legacy token and utility names are absent without embedding the forbidden full strings contiguously under `apps/web/src`.
  </behavior>
  <action>
    Create `apps/web/src/styles/token-contract.test.ts` using Vitest and Node file reads. Assert required token names, required `@theme inline` mappings, `components.json` values, package dependency presence, and dark/density contracts. Build the forbidden-token regex from fragments such as `["bg", "paper", "ink"]` and prefixes such as `"--"` or `"bg-"`; do not write forbidden full literals like the old CSS variable or utility names contiguously in the test file, because the final grep gate must not self-match tests. Include explicit failure messages for missing semantic tokens, missing preset values, missing package dependencies, and forbidden legacy bridge strings.
  </action>
  <acceptance_criteria>
    The test fails if required semantic tokens, preset fields, package declarations, `@theme inline`, `[data-theme="dark"]`, density row-height variables, or legacy-name absence regress. The test itself does not trigger the final legacy-token grep.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test -- src/styles/token-contract.test.ts</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>A deterministic token/config test covers TOKEN-01, TOKEN-03, TOKEN-05, and TOKEN-06 without weakening the final grep gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token contract to browser rendering | Global variables control all app surfaces. |
| persisted UI preferences to CSS selectors | Zustand preferences write `data-theme` and app-shell `data-density`. |
| build-time Tailwind scan to generated utilities | `@theme inline` controls which semantic utilities exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | legacy token bridge | mitigate | Delete Tailwind config bridge, remove `@config`, add token-contract test, and require final legacy grep. |
| T-06-04 | Denial of Use | focus and contrast states | mitigate | Preserve `:focus-visible` with `--ring`, non-text boundary tokens, semantic status extensions, and density row heights. |
| T-06-02 | Information Disclosure | local browser data | transfer | Browser proof is in Plan 06-06 and must use seeded E2E data only. |
| T-06-01 | Tampering | package-provided CSS imports | mitigate | Depends on Plan 06-02, which depends on package checkpoint 06-01. |
</threat_model>

<verification>
Run `corepack pnpm --filter @jobhunter/web test -- src/styles/token-contract.test.ts`, `corepack pnpm web:check`, `corepack pnpm dlx shadcn@latest info -c apps/web`, and `test ! -e apps/web/tailwind.config.ts`.
</verification>

<success_criteria>
The public token contract is standard shadcn semantic CSS variables plus clean app semantic extensions, and no Phase 6 code path relies on legacy Tailwind config colors or old public token names.
</success_criteria>

<output>
Create `.planning/phases/06-token-foundation-shadcn-preset-contract/06-03-SUMMARY.md` when done.
</output>
