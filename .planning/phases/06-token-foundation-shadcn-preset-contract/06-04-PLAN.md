---
phase: 06-token-foundation-shadcn-preset-contract
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - apps/web/src/shared/ui/button.tsx
  - apps/web/src/shared/ui/badge.tsx
  - apps/web/src/shared/ui/checkbox.tsx
  - apps/web/src/shared/ui/switch.tsx
  - apps/web/src/shared/ui/input.tsx
  - apps/web/src/shared/ui/textarea.tsx
  - apps/web/src/shared/ui/card.tsx
  - apps/web/src/shared/ui/skeleton.tsx
  - apps/web/src/shared/ui/copyable-command.tsx
  - apps/web/src/shared/ui/table.tsx
  - apps/web/src/shared/ui/tabs.tsx
  - apps/web/.storybook/preview.tsx
  - apps/web/src/contexts/profile/components/ResumeImportWizard.stories.tsx
requirements: [TOKEN-02, TOKEN-05, TOKEN-06]
autonomous: true
must_haves:
  truths:
    - "Core primitives use shadcn semantic utilities generated by Plan 06-03."
    - "D-11 and D-12 mechanical cleanup removes legacy utility names without primitive redesign."
    - "Storybook's provider wrapper uses standard background/text utilities."
  artifacts:
    - path: "apps/web/src/shared/ui/button.tsx"
      provides: "Semantic action primitive classes"
    - path: "apps/web/src/shared/ui/input.tsx"
      provides: "Semantic form-control classes"
    - path: "apps/web/.storybook/preview.tsx"
      provides: "Storybook wrapper using standard semantic utilities"
  key_links:
    - from: "apps/web/src/shared/ui/*"
      to: "apps/web/src/styles/globals.css"
      via: "Tailwind semantic class names"
      pattern: "bg-primary|bg-background|border-border|ring-ring"
---

<objective>
Mechanically migrate core shared primitives and Storybook wrapper references from legacy utility names to standard shadcn semantic utilities.

Purpose: TOKEN-02 and TOKEN-06 require that Tailwind can generate semantic utilities and that legacy utility names are absent from Phase 6 public styling. This plan handles action, form, table, card, and wrapper primitives without changing behavior or ownership.
Output: Core primitive class strings speak `background`, `foreground`, `primary`, `secondary`, `muted`, `border`, `input`, and `ring` semantics.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
@apps/web/src/shared/lib/cn.ts
@apps/web/src/shared/ui/button.tsx
@apps/web/src/shared/ui/input.tsx
@apps/web/.storybook/preview.tsx
</context>

## Artifacts this phase produces

- Core primitive class mappings: `bg-primary`, `text-primary-foreground`, `bg-destructive`, `bg-background`, `bg-card`, `text-card-foreground`, `bg-secondary`, `text-secondary-foreground`, `bg-muted`, `text-muted-foreground`, `border-border`, `border-input`, `ring-ring`, and `ring-offset-background`.
- Storybook wrapper class mapping: `bg-background text-foreground`.
- No new primitive variants, ARIA changes, route changes, query changes, or domain imports.

<tasks>

<task type="auto">
  <name>Task 1: Migrate action and state primitives</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
    apps/web/src/shared/ui/button.tsx
    apps/web/src/shared/ui/badge.tsx
    apps/web/src/shared/ui/checkbox.tsx
    apps/web/src/shared/ui/switch.tsx
  </read_first>
  <files>apps/web/src/shared/ui/button.tsx, apps/web/src/shared/ui/badge.tsx, apps/web/src/shared/ui/checkbox.tsx, apps/web/src/shared/ui/switch.tsx</files>
  <action>
    Per D-11 and D-12, mechanically replace legacy utility classes only. In `button.tsx`, set base focus to `focus-visible:ring-ring` and variants to `default: bg-primary text-primary-foreground hover:bg-primary/90`, `destructive: bg-destructive text-white hover:bg-destructive/90`, `outline: border border-input bg-background hover:bg-accent hover:text-accent-foreground`, `secondary: bg-secondary text-secondary-foreground hover:bg-secondary/80`, `ghost: hover:bg-accent hover:text-accent-foreground`, and `link: text-primary underline-offset-4 hover:underline`. In `badge.tsx`, map focus to `focus:ring-ring`, default to primary, secondary to secondary, destructive to destructive, and outline to `border-border text-foreground`. In `checkbox.tsx`, map border to `border-input`, focus to `ring-ring`, checked state to `bg-primary text-primary-foreground`. In `switch.tsx`, map focus to `ring-ring`, checked track to `bg-primary`, unchecked track to `bg-muted`, and thumb to `bg-background`. Do not change exports, Radix component wiring, cva structure, accessible labels, sizing, or behavior.
  </action>
  <acceptance_criteria>
    These files contain no `bg-paper`, `text-ink`, `border-rule`, `ring-info`, old status utility names, or old token-specific ring offsets. Existing exports and variant names are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:check</automated>
    <automated>! rg -n -P -- '\\b(?:bg|text|border|ring|ring-offset)-(?:bg|paper|paper-2|ink|rule|rule-2|danger|warn|ok|info|soft|muted)(?!-)' apps/web/src/shared/ui/button.tsx apps/web/src/shared/ui/badge.tsx apps/web/src/shared/ui/checkbox.tsx apps/web/src/shared/ui/switch.tsx</automated>
  </verify>
  <done>Action and state primitives use standard semantic utilities while preserving the same component API.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate form, card, skeleton, and command-copy surfaces</name>
  <read_first>
    apps/web/src/shared/ui/input.tsx
    apps/web/src/shared/ui/textarea.tsx
    apps/web/src/shared/ui/card.tsx
    apps/web/src/shared/ui/skeleton.tsx
    apps/web/src/shared/ui/copyable-command.tsx
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
  </read_first>
  <files>apps/web/src/shared/ui/input.tsx, apps/web/src/shared/ui/textarea.tsx, apps/web/src/shared/ui/card.tsx, apps/web/src/shared/ui/skeleton.tsx, apps/web/src/shared/ui/copyable-command.tsx</files>
  <action>
    Replace field and surface legacy classes with the shadcn standard target. Inputs and textareas use `border-input`, `bg-background`, muted hint text through Tailwind's input-hint variant, `focus-visible:ring-ring`, and `focus-visible:ring-offset-background`. Cards use `border-border bg-card text-card-foreground` and descriptions use `text-muted-foreground`. Skeleton uses `bg-muted`. Copyable command uses `border-border bg-muted` while preserving `font-mono` and clipboard behavior. Do not call browser APIs directly and do not alter clipboard port usage outside existing component behavior.
  </action>
  <acceptance_criteria>
    The five files contain only standard semantic utilities for surfaces, borders, muted text, focus rings, and monospace text. Component props and exports are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:check</automated>
    <automated>! rg -n -P -- '\\b(?:bg|text|border|ring|ring-offset)-(?:bg|paper|paper-2|ink|rule|rule-2|danger|warn|ok|info|soft|muted)(?!-)' apps/web/src/shared/ui/input.tsx apps/web/src/shared/ui/textarea.tsx apps/web/src/shared/ui/card.tsx apps/web/src/shared/ui/skeleton.tsx apps/web/src/shared/ui/copyable-command.tsx</automated>
  </verify>
  <done>Core surface and form primitives no longer reference legacy utility classes.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate table, tabs, and Storybook wrapper references</name>
  <read_first>
    apps/web/src/shared/ui/table.tsx
    apps/web/src/shared/ui/tabs.tsx
    apps/web/.storybook/preview.tsx
    apps/web/src/contexts/profile/components/ResumeImportWizard.stories.tsx
    docs/local-reliability-qa.md
  </read_first>
  <files>apps/web/src/shared/ui/table.tsx, apps/web/src/shared/ui/tabs.tsx, apps/web/.storybook/preview.tsx, apps/web/src/contexts/profile/components/ResumeImportWizard.stories.tsx</files>
  <action>
    Map tables to `border-border`, `bg-muted/50`, `text-muted-foreground`, and `hover:bg-muted/50`. Map tabs list to `bg-muted text-muted-foreground`, active trigger to `bg-background text-foreground`, focus to `ring-ring`, and ring offset to `ring-offset-background`. In Storybook preview, replace wrapper `bg-bg text-ink` with `bg-background text-foreground`. In `ResumeImportWizard.stories.tsx`, replace `text-muted` with `text-muted-foreground`. Preserve Storybook providers, MSW handlers, a11y parameters, and story behavior.
  </action>
  <acceptance_criteria>
    Table/tabs primitives and Storybook wrappers use standard semantic utilities and still compile with existing stories. No Storybook a11y deferral is added or removed in this phase.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:check</automated>
    <automated>! rg -n -P -- '\\b(?:bg|text|border|ring|ring-offset)-(?:bg|paper|paper-2|ink|rule|rule-2|danger|warn|ok|info|soft|muted)(?!-)' apps/web/src/shared/ui/table.tsx apps/web/src/shared/ui/tabs.tsx apps/web/.storybook/preview.tsx apps/web/src/contexts/profile/components/ResumeImportWizard.stories.tsx</automated>
  </verify>
  <done>Core table/tabs primitives and Storybook wrapper references are clean of legacy utility names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared primitives to all views | Core primitives render across forms, cards, tables, tabs, and Storybook. |
| Storybook providers to visual QA | Storybook wrapper controls theme-visible backgrounds for component review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | legacy utility references | mitigate | Per-file grep after each task and final phase grep in 06-06. |
| T-06-04 | Denial of Use | focus rings and control boundaries | mitigate | Use `ring-ring`, `border-input`, `border-border`, and keep existing sizing/ARIA. |
| T-06-02 | Information Disclosure | Storybook stories | mitigate | Use existing synthetic story data only; do not add real profile, resume, job, or artifact data. |
</threat_model>

<verification>
Run `corepack pnpm web:check` after each task and the listed per-file grep commands. Plan 06-06 runs the phase-wide grep and browser smoke.
</verification>

<success_criteria>
Core primitives and Storybook wrapper references no longer depend on legacy utility names, and behavior/exports stay unchanged.
</success_criteria>

<output>
Create `.planning/phases/06-token-foundation-shadcn-preset-contract/06-04-SUMMARY.md` when done.
</output>
