---
phase: 06-token-foundation-shadcn-preset-contract
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/components.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/styles/globals.css
requirements: [TOKEN-03, TOKEN-04]
autonomous: true
must_haves:
  truths:
    - "D-06 keeps `shadcn` as a dependency and `shadcn/tailwind.css` remains imported."
    - "D-08 uses shadcn CLI only for `apply b3F5kqmYd8 --only theme` after alias validation."
    - "D-10 final config points aliases under `@/shared/*` and Tabler/luma preset fields are represented."
  artifacts:
    - path: "apps/web/package.json"
      provides: "Preset dependency declarations"
    - path: "pnpm-lock.yaml"
      provides: "Resolved dependency lock entries"
    - path: "apps/web/components.json"
      provides: "shadcn Tailwind v4 preset contract"
    - path: "apps/web/vite.config.ts"
      provides: "Vite `@` alias"
    - path: "apps/web/tsconfig.json"
      provides: "TypeScript `@/*` path alias"
  key_links:
    - from: "apps/web/components.json"
      to: "apps/web/src/shared/ui"
      via: "aliases.ui"
      pattern: "\"ui\": \"@/shared/ui\""
    - from: "apps/web/vite.config.ts"
      to: "apps/web/src"
      via: "resolve.alias @"
      pattern: "\"@\".*new URL\\(\"./src\""
---

<objective>
Install the preset-level dependencies and make shadcn/Vite/TypeScript config validate before token implementation continues.

Purpose: TOKEN-03 and TOKEN-04 require the decoded preset, dependency baseline, aliases, and shadcn CLI contract to be real project configuration, not research notes. This plan executes those prerequisites after Plan 06-01 approval.
Output: Updated dependency files, valid `@/*` aliases, `components.json` target fields, and theme-only shadcn CLI output ready for Plan 06-03 adaptation.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
@.planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
@apps/web/package.json
@apps/web/components.json
@apps/web/vite.config.ts
@apps/web/tsconfig.json
@apps/web/src/styles/globals.css
</context>

## Artifacts this phase produces

- Dependency entries: `shadcn@4.11.0`, `tw-animate-css@1.4.0`, `@fontsource-variable/geist@5.2.9`, `@fontsource-variable/jetbrains-mono@5.2.8`, `@tabler/icons-react@3.44.0`, and dev dependency `@types/node@25.9.2`.
- `components.json` target fields: `style: "radix-luma"`, `iconLibrary: "tabler"`, `tailwind.css: "src/styles/globals.css"`, `tailwind.baseColor: "neutral"`, `tailwind.cssVariables: true`, `tailwind.prefix: ""`, `tailwind.config: ""`, aliases under `@/shared/*`, and preset menu fields `menuColor: "default-translucent"` plus `menuAccent: "subtle"` when accepted by the current schema.
- Vite `resolve.alias` mapping `"@"` to `apps/web/src`.
- TypeScript `compilerOptions.baseUrl` set to `"."` and `paths["@/*"]` set to `["src/*"]`.
- Theme-only shadcn apply output from `b3F5kqmYd8`; no component rewrite output is accepted.

<tasks>

<task type="auto">
  <name>Task 1: Install approved preset dependencies</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-RESEARCH.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-CONTEXT.md
    apps/web/package.json
    package.json
  </read_first>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <action>
    After Plan 06-01 approval, install the approved preset packages per D-06 and D-07. Use `corepack pnpm --filter @jobhunter/web add shadcn@4.11.0 tw-animate-css@1.4.0 @fontsource-variable/geist@5.2.9 @fontsource-variable/jetbrains-mono@5.2.8 @tabler/icons-react@3.44.0`. Then use `corepack pnpm --filter @jobhunter/web add -D @types/node@25.9.2`. Do not add `tailwindcss-animate`; use `tw-animate-css` per research. Do not remove `lucide-react` in Phase 6 because user-visible icon migration is Phase 8, while `components.json` still targets Tabler for new shadcn output per D-10.
  </action>
  <acceptance_criteria>
    `apps/web/package.json` contains the six target packages in the correct dependency/devDependency sections, and `pnpm-lock.yaml` contains resolved importer entries for them. No unrelated dependency group is reorganized.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web list shadcn tw-animate-css @fontsource-variable/geist @fontsource-variable/jetbrains-mono @tabler/icons-react @types/node --depth 0</automated>
  </verify>
  <done>Approved dependencies are installed and locked without adding deprecated animation packages or removing existing icon compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Repair aliases and apply the theme-only preset</name>
  <read_first>
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-PATTERNS.md
    .planning/phases/06-token-foundation-shadcn-preset-contract/06-UI-SPEC.md
    apps/web/components.json
    apps/web/vite.config.ts
    apps/web/tsconfig.json
    apps/web/src/styles/globals.css
  </read_first>
  <files>apps/web/components.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/src/styles/globals.css</files>
  <action>
    Add Vite `resolve.alias` so `"@"` resolves to `fileURLToPath(new URL("./src", import.meta.url))` without changing TanStack Router, React, Tailwind, proxy, root, or build settings. Add TypeScript `compilerOptions.baseUrl: "."` and `paths: { "@/*": ["src/*"] }` without weakening existing strict flags. Update `apps/web/components.json` per D-10 to the Tailwind v4 target: `style` is `radix-luma`, `iconLibrary` is `tabler`, `tailwind.css` remains `src/styles/globals.css`, `tailwind.baseColor` is `neutral`, `tailwind.cssVariables` is `true`, `tailwind.prefix` is `""`, and `tailwind.config` is `""`. Preserve `rsc: false`, `tsx: true`, and all `@/shared/*` aliases. Run `corepack pnpm dlx shadcn@latest info -c apps/web`, then run exactly `corepack pnpm dlx shadcn@latest apply b3F5kqmYd8 --only theme -y -c apps/web` per D-08. Do not run uncontrolled full apply per D-09. If the CLI writes under `apps/web/src/shared/ui`, stop and report because Phase 6 accepts theme output only.
  </action>
  <acceptance_criteria>
    `shadcn info -c apps/web` resolves `@/shared/ui`, `@/shared/lib/cn`, and `@/shared/hooks` under `apps/web/src`, not `apps/web/@`. `git diff --name-only` after theme-only apply contains no `apps/web/src/shared/ui/*` component rewrites. The config represents `b3F5kqmYd8` target values: radix-luma/luma, neutral, sky, amber chart palette through theme output, medium radius, Geist, JetBrains Mono, Tabler, default-translucent menu, and subtle menu accent.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm dlx shadcn@latest info -c apps/web</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>Aliases validate, `components.json` points to the final shadcn contract, and the only shadcn CLI apply accepted is theme-only output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local dependency tree | Approved packages enter the build and lockfile. |
| shadcn CLI to repository files | CLI output may alter config and CSS. |
| build config to frontend source | Vite and TypeScript alias resolution controls import paths for generated/copied components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | dependency install and `shadcn@latest` dlx | mitigate | Plan depends on blocking package approval from 06-01; install only named approved versions; no unreviewed postinstall behavior. |
| T-06-SC | Tampering | package-manager install | mitigate | Package Legitimacy Audit exists; SUS packages are gated; `[SLOP]` packages are absent. |
| T-06-03 | Tampering | shadcn apply surface | mitigate | Use `--only theme`; reject component rewrites under `apps/web/src/shared/ui`. |
| T-06-04 | Information Disclosure | config changes | accept | No runtime data or screenshots are handled in this plan. |
</threat_model>

<verification>
Run `corepack pnpm dlx shadcn@latest info -c apps/web` and `corepack pnpm web:check`. Confirm no full component apply occurred.
</verification>

<success_criteria>
The dependency/config preset foundation is present and Plan 06-03 can implement the CSS-first token contract without alias or CLI validation failures.
</success_criteria>

<output>
Create `.planning/phases/06-token-foundation-shadcn-preset-contract/06-02-SUMMARY.md` when done.
</output>
