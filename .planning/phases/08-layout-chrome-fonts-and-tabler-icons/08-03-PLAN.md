---
phase: 08-layout-chrome-fonts-and-tabler-icons
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - apps/web/src/shared/layout/Topbar.test.tsx
  - apps/web/src/shared/layout/ThemeToggle.test.tsx
  - apps/web/src/shared/layout/ConnectionStatusPill.test.tsx
  - apps/web/src/styles/token-contract.test.ts
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
user_setup: []
must_haves:
  truths:
    - "Global search still ignores blank input and navigates non-empty Enter to `/jobs` with trimmed `q` and `page=1`."
    - "Theme and density controls keep accessible names and update the existing UI preference store."
    - "Connection status keeps operational labels, live-region semantics, and warning banner roles."
  artifacts:
    - path: "apps/web/src/shared/layout/Topbar.test.tsx"
      provides: "Global search and density-control regression coverage"
      exports: []
    - path: "apps/web/src/shared/layout/ThemeToggle.test.tsx"
      provides: "Theme toggle accessible-name and store-update regression coverage"
      exports: []
    - path: "apps/web/src/shared/layout/ConnectionStatusPill.test.tsx"
      provides: "Connection pill labels, aria-live, and banner role coverage"
      exports: []
  key_links:
    - from: "apps/web/src/shared/layout/Topbar.tsx"
      to: "apps/web/src/shared/layout/Topbar.test.tsx"
      via: "URL and density assertions"
      pattern: "Global search|Row density|/jobs"
    - from: "apps/web/src/shared/layout/ConnectionStatusPill.tsx"
      to: "apps/web/src/shared/layout/ConnectionStatusPill.test.tsx"
      via: "role/name/live-region assertions"
      pattern: "aria-live|role=\"alert\"|role=\"status\""
---

<objective>
Add focused shell behavior tests so the visual/icon migration cannot regress navigation, preference, or status behavior.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-CONTEXT.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-VALIDATION.md
@apps/web/src/test/render.tsx
@apps/web/src/test/testPorts.ts
@apps/web/src/shared/layout/Topbar.tsx
@apps/web/src/shared/layout/ThemeToggle.tsx
@apps/web/src/shared/layout/ConnectionStatusPill.tsx
@apps/web/src/shared/stores/ui-preferences.ts
@apps/web/src/styles/token-contract.test.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | LAYOUT-01 | Shell workflow behavior unchanged | 08-03 | COVERED | Tests focus on search, nav, theme, density, and status behavior.
REQ | LAYOUT-02 | Font contract remains wired | 08-03 | COVERED | Token contract assertions cover font imports/mappings.
REQ | LAYOUT-03 | Icon swap does not change accessible names | 08-03 | COVERED | Theme/shared controls remain discoverable by role/name.
REQ | LAYOUT-04 | Density behavior remains store/shell driven | 08-03 | COVERED | Density test updates store and shell proof follows in 08-04.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test Topbar search, nav labels, and density control</name>
  <files>apps/web/src/shared/layout/Topbar.test.tsx, apps/web/src/shared/layout/Topbar.tsx</files>
  <read_first>
    @apps/web/src/shared/layout/Topbar.tsx
    @apps/web/src/shared/layout/NavBar.tsx
    @apps/web/src/test/render.tsx
    @apps/web/src/shared/stores/ui-preferences.ts
  </read_first>
  <behavior>
    - Blank or whitespace-only Enter in the global search does not navigate.
    - Non-empty Enter trims the query and navigates to `/jobs` with `q` and `page=1`.
    - Main navigation labels remain visible with the existing route names.
    - Row density select exposes compact, regular, and comfy and updates `useUiPreferencesStore`.
  </behavior>
  <action>Add a focused `Topbar.test.tsx` using existing Testing Library and TanStack Router memory-history patterns. Reset `useUiPreferencesStore` between tests. Assert behavior through role/name and URL/search state, not implementation internals. Do not add real API calls, EventSource, localStorage reads, or worker-backed operations.</action>
  <acceptance_criteria>
    - Tests would fail if `/jobs` navigation drops `q`, trimming, or `page=1`.
    - Tests would fail if density options or accessible name change.
    - Tests use synthetic in-memory route state only.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/shared/layout/Topbar.test.tsx</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>Topbar behavior is regression-tested independently of visual styling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test ThemeToggle accessibility and preference update</name>
  <files>apps/web/src/shared/layout/ThemeToggle.test.tsx, apps/web/src/shared/layout/ThemeToggle.tsx</files>
  <read_first>
    @apps/web/src/shared/layout/ThemeToggle.tsx
    @apps/web/src/shared/providers/ThemeProvider.tsx
    @apps/web/src/shared/stores/ui-preferences.ts
    @apps/web/src/test/render.tsx
  </read_first>
  <behavior>
    - Initial light state exposes a button named `Switch to dark theme`.
    - Clicking toggles store theme to dark and the button name becomes `Switch to light theme`.
    - The icon remains decorative; the visible control remains discoverable by button name.
  </behavior>
  <action>Add `ThemeToggle.test.tsx` around the existing provider harness. Reset the UI preference store between tests and assert accessible names/store state. Do not assert Tabler implementation details beyond preserving a named button.</action>
  <acceptance_criteria>
    - The test fails if the accessible name disappears.
    - The test fails if `setTheme(next)` behavior changes.
    - No direct localStorage contract is added to the component.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/shared/layout/ThemeToggle.test.tsx</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>ThemeToggle behavior remains accessible after the Tabler icon swap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test ConnectionStatusPill live-region and banner roles</name>
  <files>apps/web/src/shared/layout/ConnectionStatusPill.test.tsx, apps/web/src/shared/layout/ConnectionStatusPill.tsx</files>
  <read_first>
    @apps/web/src/shared/layout/ConnectionStatusPill.tsx
    @apps/web/src/contexts/operations/providers/EventStreamProvider.tsx
    @apps/web/src/contexts/operations/hooks/useHealthQuery.ts
    @apps/web/src/test/render.tsx
    @apps/web/src/test/testPorts.ts
  </read_first>
  <behavior>
    - Open status renders `live` with `aria-live=\"polite\"`.
    - Worker-unhealthy health data renders `worker` plus a banner with `role=\"alert\"`.
    - Long closed/connecting status renders `offline` plus a banner with `role=\"status\"` when timers are controlled.
  </behavior>
  <action>Add `ConnectionStatusPill.test.tsx` using mocked health/event-stream seams or the existing provider harness. Prefer deterministic fake timers for long-disconnect behavior; if the provider cannot be mocked safely, cover worker-unhealthy and live status in Vitest and leave long-disconnect proof to Playwright in 08-04. Do not start a real Temporal worker, SSE stream, or API server.</action>
  <acceptance_criteria>
    - Tests prove live-region and banner role semantics for the states that are practical in unit tests.
    - Any state not covered in Vitest is explicitly listed in `08-03-SUMMARY.md` and covered by 08-04 browser proof.
    - No real worker-backed job or API state is used.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/shared/layout/ConnectionStatusPill.test.tsx</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>ConnectionStatusPill has behavior-level regression coverage for status labels and accessibility semantics.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-T | Tampering | Test harness routing | mitigate | Use memory router/provider patterns and assert public URL/search behavior. |
| T-08-03-E | Elevation of Privilege | Connection tests | mitigate | Mock status/health seams; do not start workers, SSE servers, or product workflows. |
| T-08-03-I | Information Disclosure | Test fixtures | mitigate | Synthetic labels and status data only; no user artifacts or logs. |
</threat_model>

<verification>
- `corepack pnpm --filter @jobhunter/web test src/shared/layout/Topbar.test.tsx src/shared/layout/ThemeToggle.test.tsx src/shared/layout/ConnectionStatusPill.test.tsx src/styles/token-contract.test.ts`
- `corepack pnpm web:check`
- `git diff --check`
</verification>

<success_criteria>
Focused tests prove the shell migration preserved search, theme, density, icon-accessibility, and connection-status behavior.
</success_criteria>

<output>
Create `.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-03-SUMMARY.md` when done.
</output>
