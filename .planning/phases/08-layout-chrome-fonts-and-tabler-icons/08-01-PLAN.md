---
phase: 08-layout-chrome-fonts-and-tabler-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/shared/layout/AppShell.tsx
  - apps/web/src/shared/layout/Topbar.tsx
  - apps/web/src/shared/layout/NavBar.tsx
  - apps/web/src/shared/layout/ThemeToggle.tsx
  - apps/web/src/shared/layout/ConnectionStatusPill.tsx
  - apps/web/src/styles/globals.css
  - apps/web/src/styles/tokens.css
  - apps/web/src/styles/token-contract.test.ts
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-04
  - LAYOUT-05
user_setup: []
must_haves:
  truths:
    - "AppShell keeps `.app-shell` and `data-density={density}` as the density root."
    - "Topbar, NavBar, global search, theme toggle, density select, and connection status behavior remain route/store/provider driven."
    - "Shell chrome consumes standard shadcn semantic tokens and keeps Geist/JetBrains Mono font mappings."
  artifacts:
    - path: "apps/web/src/styles/globals.css"
      provides: "Tokenized topbar, nav, tab, input/select, connection pill, and banner chrome"
      exports: []
    - path: "apps/web/src/styles/tokens.css"
      provides: "Font variables and app-shell density variables"
      exports: []
    - path: "apps/web/src/styles/token-contract.test.ts"
      provides: "Static contract for shadcn tokens, fonts, density, and shell chrome selectors"
      exports: []
  key_links:
    - from: "apps/web/src/shared/layout/AppShell.tsx"
      to: "apps/web/src/styles/tokens.css"
      via: "data-density drives --jh-row-height"
      pattern: "data-density|--jh-row-height"
    - from: "apps/web/src/shared/layout/Topbar.tsx"
      to: "apps/web/src/styles/globals.css"
      via: "topbar/nav/global-search/select/tab/connection classes"
      pattern: "topbar|global-search|connection-pill"
---

<objective>
Retokenize the app shell chrome and font/density contract without changing route, preference, search, or connection-status behavior.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-CONTEXT.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-UI-SPEC.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-RESEARCH.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-PATTERNS.md
@.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-VALIDATION.md
@apps/web/src/shared/layout/AppShell.tsx
@apps/web/src/shared/layout/Topbar.tsx
@apps/web/src/shared/layout/NavBar.tsx
@apps/web/src/shared/layout/ThemeToggle.tsx
@apps/web/src/shared/layout/ConnectionStatusPill.tsx
@apps/web/src/styles/globals.css
@apps/web/src/styles/tokens.css
@apps/web/src/styles/token-contract.test.ts
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | LAYOUT-01 | Shell/topbar/nav/search/theme/density/status/tabs adopt preset visual language without workflow changes | 08-01 | COVERED | CSS/token work stays on existing shell selectors and components.
REQ | LAYOUT-02 | Geist and JetBrains Mono load in Vite and Storybook without dense-layout breakage | 08-01 | COVERED | Font imports and mappings remain in global CSS/token contract.
REQ | LAYOUT-04 | Compact/regular/comfy density still affects row height | 08-01 | COVERED | `.app-shell[data-density]` and `--jh-row-height` remain canonical.
REQ | LAYOUT-05 | Topbar/menu translucency remains readable | 08-01, 08-04 | PARTIAL | This plan creates readable shell surfaces; browser proof lands in 08-04.
CONTEXT | D-13 | Preserve global search route contract | 08-01, 08-03 | COVERED | Component behavior stays unchanged; tests in 08-03.
CONTEXT | D-14 | Do not hide connection warning/offline states | 08-01, 08-03 | COVERED | Keep status labels, data-status, live regions, and banner roles.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Retokenize topbar, nav, tabs, and form chrome</name>
  <files>apps/web/src/styles/globals.css, apps/web/src/shared/layout/AppShell.tsx, apps/web/src/shared/layout/Topbar.tsx, apps/web/src/shared/layout/NavBar.tsx</files>
  <read_first>
    @apps/web/src/shared/layout/AppShell.tsx
    @apps/web/src/shared/layout/Topbar.tsx
    @apps/web/src/shared/layout/NavBar.tsx
    @apps/web/src/styles/globals.css
    @.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-UI-SPEC.md
  </read_first>
  <action>Update only visual shell selectors in `globals.css` so `.topbar`, `.brand`, `.brand-mark`, `.nav`, `.tab`, `.global-search`, `.select`, `select`, `input`, and `textarea` consume shadcn semantic tokens (`--card`, `--popover`, `--muted`, `--accent`, `--border`, `--input`, `--ring`, `--foreground`, `--muted-foreground`). Preserve `AppShell`, `Topbar`, and `NavBar` behavior and route labels. Do not add route groups, new product copy, direct platform calls, gradients, hero chrome, or domain/status remapping.</action>
  <acceptance_criteria>
    - Route labels and `activeProps={{ className: "on" }}` remain unchanged.
    - Global search placeholder and Enter navigation implementation remain unchanged.
    - Shell selectors use standard semantic tokens and no legacy token names.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/styles/token-contract.test.ts</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>Topbar/nav/tab/input chrome is tokenized without shell component behavior changes.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve font and density contracts with static assertions</name>
  <files>apps/web/src/styles/tokens.css, apps/web/src/styles/globals.css, apps/web/src/styles/token-contract.test.ts</files>
  <read_first>
    @apps/web/src/styles/tokens.css
    @apps/web/src/styles/globals.css
    @apps/web/src/styles/token-contract.test.ts
    @apps/web/.storybook/preview.tsx
  </read_first>
  <action>Keep Fontsource imports, `--jh-font-sans`, `--jh-font-heading`, `--jh-font-mono`, Tailwind `--font-*` mappings, and `.app-shell[data-density]` row-height values intact. Add narrow token-contract assertions if shell/font/density selectors are changed. Do not move density to `<html>` or add viewport-width font scaling.</action>
  <acceptance_criteria>
    - `--jh-row-height` values remain 32px, 40px, and 48px.
    - `@fontsource-variable/geist` and `@fontsource-variable/jetbrains-mono` remain imported through global CSS.
    - Token contract test covers any new shell/font/density contract introduced by this plan.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/styles/token-contract.test.ts</automated>
    <automated>corepack pnpm web:check</automated>
  </verify>
  <done>Static tests prove the shell continues using the standard font and density seams.</done>
</task>

<task type="auto">
  <name>Task 3: Keep connection status chrome readable and honest</name>
  <files>apps/web/src/shared/layout/ConnectionStatusPill.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    @apps/web/src/shared/layout/ConnectionStatusPill.tsx
    @apps/web/src/styles/globals.css
    @.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-CONTEXT.md
  </read_first>
  <action>Update only the connection pill and banner visual treatment. Preserve `STATUS_LABEL`, `CONNECTION_LOST_THRESHOLD_MS`, health query usage, EventStream status usage, data-status mapping, `aria-live`, `role=\"alert\"`, and `role=\"status\"`. Use semantic success/warning/destructive/status tokens already defined by Phase 6; do not soften or hide worker/offline copy.</action>
  <acceptance_criteria>
    - `live`, `connecting`, `reconnecting`, `offline`, and `worker` labels remain possible.
    - Worker banner still has `role=\"alert\"` and long disconnect banner still has `role=\"status\"`.
    - Destructive/offline surfaces remain visibly distinct.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm web:check</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Connection status chrome is tokenized while preserving operational status honesty.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-T | Tampering | Topbar routing/search | mitigate | Keep TanStack Router `Link` and `useNavigate` code unchanged; Plan 08-03 adds regression tests. |
| T-08-01-R | Repudiation | Connection status | mitigate | Preserve labels, `aria-live`, banner roles, and status data attributes. |
| T-08-01-I | Information Disclosure | Visual QA evidence | mitigate | This plan records only command results and synthetic shell behavior; no screenshots or sensitive data. |
</threat_model>

<verification>
- `corepack pnpm --filter @jobhunter/web test src/styles/token-contract.test.ts`
- `corepack pnpm web:check`
- `git diff --check`
</verification>

<success_criteria>
The app shell uses shadcn semantic chrome tokens and the existing font/density seams while preserving shell route, search, preference, and connection-status behavior.
</success_criteria>

<output>
Create `.planning/phases/08-layout-chrome-fonts-and-tabler-icons/08-01-SUMMARY.md` when done.
</output>
