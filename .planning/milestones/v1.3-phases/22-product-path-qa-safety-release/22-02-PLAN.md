---
phase: 22-product-path-qa-safety-release
plan: "02"
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - workers/automation/tests/test_market_compensation_estimator.py
  - workers/automation/src/jobhunter/domain/compensation/market.py
  - apps/api/test/market-compensation-estimates.test.ts
  - apps/api/test/projections.test.ts
  - apps/api/src/projections.ts
requirements:
  - QA-01
  - QA-03
  - QA-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Backend tests prove trimodal fallback, inferred trimodal tier, low sample, weak level, weak location, and source dispersion degrade market confidence."
    - "API/projection tests prove trimodal fallback, source conflict, and low-confidence evidence are served from canonical rows."
    - "No backend/API release fixture calls live compensation providers or exposes unsafe source payloads."
  artifacts:
    - path: "workers/automation/tests/test_market_compensation_estimator.py"
      provides: "Weak market evidence regression tests"
      contains: "test_estimates_trimodal_tier_role_fallback_with_explicit_warning"
    - path: "apps/api/test/market-compensation-estimates.test.ts"
      provides: "Canonical API evidence for fallback and conflict states"
      contains: "serves trimodal fallback and source-conflict evidence from canonical market rows"
    - path: "apps/api/test/projections.test.ts"
      provides: "Projection evidence from canonical compensation rows"
      contains: "trimodal tier role fallback"
  key_links:
    - from: "workers/automation/tests/test_market_compensation_estimator.py"
      to: "workers/automation/src/jobhunter/domain/compensation/market.py"
      via: "estimate_market_compensation"
      pattern: "estimate_market_compensation"
    - from: "apps/api/test/projections.test.ts"
      to: "apps/api/src/projections.ts"
      via: "refreshProjections and canonical SQLite rows"
      pattern: "job_market_compensation_estimates"
---

<objective>
Close backend/API/projection evidence gaps surfaced by the Phase 22 release matrix.

Purpose: QA-03 and QA-04 need direct proof that weak compensation evidence degrades confidence and that API/projection output stays sourced from canonical rows. This implements D-03, D-05, D-06, D-08, D-09, D-11, D-12, D-14.
Output: Focused Python estimator tests plus API/projection canonical-row tests for trimodal fallback, source conflict, and weak confidence evidence.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-product-path-qa-safety-release/22-VALIDATION.md
@.planning/phases/22-product-path-qa-safety-release/22-CONTEXT.md
@.planning/phases/22-product-path-qa-safety-release/22-RESEARCH.md
@.planning/phases/22-product-path-qa-safety-release/22-PATTERNS.md
@workers/automation/tests/test_market_compensation_estimator.py
@workers/automation/src/jobhunter/domain/compensation/market.py
@apps/api/test/market-compensation-estimates.test.ts
@apps/api/test/projections.test.ts
@apps/api/src/projections.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add weak-market estimator evidence</name>
  <files>workers/automation/tests/test_market_compensation_estimator.py, workers/automation/src/jobhunter/domain/compensation/market.py</files>
  <read_first>
    - `workers/automation/tests/test_market_compensation_estimator.py`
    - `workers/automation/src/jobhunter/domain/compensation/market.py`
    - `.planning/phases/22-product-path-qa-safety-release/22-VALIDATION.md`
    - `.planning/phases/22-product-path-qa-safety-release/22-PATTERNS.md`
  </read_first>
  <behavior>
    - Test: `test_estimates_trimodal_tier_role_fallback_with_explicit_warning` uses synthetic rows where the target company supplies tier context and a same-tier company supplies the matching role; expected `estimate_state == "estimated_range"`, `match_scope == "tier_role_fallback"`, `aggregate_bucket == "trimodal tier role fallback"`, and `company_role_fallback` warning.
    - Test: `test_infers_trimodal_tier_from_reported_compensation_midpoint` uses exact company-role rows with `company_tier == "unknown"` and supported ranges; expected inferred non-unknown `company_tier`, `trimodal_tier_inferred` warning, and a `trimodal_tier` confidence factor.
    - Test: `test_weak_market_factors_degrade_confidence_without_precise_range` covers `low_sample_count`, `weak_level_match`, `weak_location_match`, and `source_dispersion_too_high`; expected `estimate_state == "insufficient_evidence"`, low or none confidence, specific factor/reason codes, and no live provider access.
  </behavior>
  <action>Add the three tests above to `workers/automation/tests/test_market_compensation_estimator.py` using existing `_levels`, `_glassdoor`, and `estimate_market_compensation` patterns. Extend `_levels` and `_glassdoor` test helpers with optional `location`, `tier`, and `level` overrides only as needed for the new synthetic cases. Run the tests RED before changing production. If a RED test exposes a domain behavior gap, update only `workers/automation/src/jobhunter/domain/compensation/market.py` to preserve existing public types while making the factor/reason/warning states explicit. Do not add provider access, network calls, scraping, profile preference editing, ranking, filtering, apply readiness, or dispatch behavior per D-02, D-08, and D-09.</action>
  <verify>
    <automated>uv --project workers/automation run --extra dev pytest -q workers/automation/tests/test_market_compensation_estimator.py</automated>
  </verify>
  <acceptance_criteria>
    - The named tests exist in `workers/automation/tests/test_market_compensation_estimator.py`.
    - Trimodal fallback asserts `match_scope == "tier_role_fallback"` and `company_role_fallback`.
    - Inferred tier asserts `trimodal_tier_inferred` and a `trimodal_tier` factor.
    - Weak market factors assert `low_sample_count`, `weak_level_match`, `weak_location_match`, and `source_dispersion_too_high`.
    - Test data uses only synthetic company, role, salary, and source rows.
  </acceptance_criteria>
  <done>QA-03 has direct Python estimator proof for trimodal and weak-factor confidence degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical API/projection release evidence</name>
  <files>apps/api/test/market-compensation-estimates.test.ts, apps/api/test/projections.test.ts, apps/api/src/projections.ts</files>
  <read_first>
    - `apps/api/test/market-compensation-estimates.test.ts`
    - `apps/api/test/projections.test.ts`
    - `apps/api/src/projections.ts`
    - `packages/contracts/src/schemas.ts`
    - `.planning/phases/22-product-path-qa-safety-release/22-VALIDATION.md`
    - `.planning/phases/22-product-path-qa-safety-release/22-PATTERNS.md`
  </read_first>
  <behavior>
    - API test: `serves trimodal fallback and source-conflict evidence from canonical market rows` inserts a synthetic `job_market_compensation_estimates` row with `match_scope = "tier_role_fallback"`, `aggregate_bucket = "trimodal tier role fallback"`, `confidence_band = "low"` or `medium` as supported by the fixture, and warnings `reported_compensation_sample`, `company_role_fallback`, `trimodal_tier_inferred`, and `source_conflict_with_posted_salary`.
    - Projection test: canonical rows in `job_posted_compensation_facts` and `job_market_compensation_estimates` refresh into job list/detail compensation JSON without frontend parsing and without unsafe payload strings.
  </behavior>
  <action>Add the API test to `apps/api/test/market-compensation-estimates.test.ts` using `withTempApp`, `insertEstimate`, and `app.inject` patterns. Add or extend a focused projection test in `apps/api/test/projections.test.ts` that seeds canonical posted and market rows, calls the existing projection refresh/read path, and asserts list `compensationSummary.market` plus detail `compensationAudit.market.estimate` preserve `matchScope`, `aggregateBucket`, `confidenceBand`, factor names, warning codes, and source snapshots. Assert serialized API/projection JSON does not contain `/Users/`, `file://`, `rawProviderPayload`, `credential`, `secret`, `api_key`, `token`, or `password`. If the RED projection/API tests expose missing canonical mapping, update only `apps/api/src/projections.ts` and existing contract schema consumers needed to pass; do not add React-side salary computation per D-03 and D-09.</action>
  <verify>
    <automated>corepack pnpm api:test -- market-compensation-estimates.test.ts projections.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API response includes `matchScope: "tier_role_fallback"` and source-conflict warning from the canonical market row.
    - Projection output includes posted and market evidence as separate sections.
    - Projection output is derived from `job_posted_compensation_facts` and `job_market_compensation_estimates`, not `jobs.salary` parsing or frontend computation.
    - Serialized compensation JSON excludes unsafe payload strings and local path markers.
  </acceptance_criteria>
  <done>QA-04 has API/projection proof that release compensation states remain canonical, parity-safe, and sanitized.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Created By |
|----------|------|------------|
| `test_estimates_trimodal_tier_role_fallback_with_explicit_warning` | Python test function | Plan 22-02 Task 1 |
| `test_infers_trimodal_tier_from_reported_compensation_midpoint` | Python test function | Plan 22-02 Task 1 |
| `test_weak_market_factors_degrade_confidence_without_precise_range` | Python test function | Plan 22-02 Task 1 |
| `_levels(..., location=..., tier=..., level=...)` helper parameters | Python test helper extension | Plan 22-02 Task 1 |
| `_glassdoor(..., location=..., tier=..., level=...)` helper parameters | Python test helper extension | Plan 22-02 Task 1 |
| `serves trimodal fallback and source-conflict evidence from canonical market rows` | API Vitest case | Plan 22-02 Task 2 |
| `projects trimodal fallback and source-conflict evidence from canonical compensation rows` | API projection Vitest case | Plan 22-02 Task 2 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic reported rows -> Python estimator | Test data drives confidence, warning, and reason calculation. |
| Canonical SQLite rows -> API/projection JSON | Persisted rows become user-facing compensation audit fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege / Safety | API tests and estimator fixtures | mitigate | Use only pure unit tests and `app.inject`; no auto-apply, browser submission, mailbox scanning, material regeneration, destructive profile/database actions, provider calls, or worker-backed apply jobs. |
| T-22-02 | Tampering | `estimate_market_compensation`, API read model | mitigate | Tests must assert weak compensation evidence degrades to warnings/insufficient evidence and never influences fit score, sorting, filtering, readiness, blockers, handoff, or dispatch controls. |
| T-22-03 | Information Disclosure | Source snapshots and serialized JSON | mitigate | Tests assert JSON excludes local paths, credentials, tokens, secrets, raw provider payloads, and private payload markers. |
| T-22-04 | Tampering | `apps/api/src/projections.ts` canonical mapping | mitigate | Projection tests seed canonical posted/market tables and assert UI/API fields come from projection JSON, not read-time parsing or frontend recomputation. |
| T-22-SC | Tampering | npm/pip/cargo installs | mitigate | No dependency install is planned; stop if a package install is proposed. |
</threat_model>

<verification>
Run `uv --project workers/automation run --extra dev pytest -q workers/automation/tests/test_market_compensation_estimator.py` and `corepack pnpm api:test -- market-compensation-estimates.test.ts projections.test.ts`. Then run `git diff --check`.
</verification>

<success_criteria>
Plan 22-02 succeeds when backend/API tests prove QA-01, QA-03, and QA-04 release states using only synthetic canonical rows and no prohibited provider/apply/material behavior.
</success_criteria>

<output>
Create `.planning/phases/22-product-path-qa-safety-release/22-02-SUMMARY.md` when done.
</output>
