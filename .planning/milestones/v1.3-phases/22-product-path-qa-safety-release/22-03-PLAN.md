---
phase: 22-product-path-qa-safety-release
plan: "03"
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - apps/web/src/test/fixtures/projections.ts
  - apps/web/src/views/jobs/JobsView.test.tsx
  - apps/web/src/views/jobs/JobDetailDrawer.test.tsx
  - apps/web/src/views/jobs/JobCompensationAuditSection.tsx
  - apps/web/e2e/tests/jobs-drawer.spec.ts
requirements:
  - QA-01
  - QA-02
  - QA-05
  - QA-06
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Jobs list and drawer tests explicitly cover source-conflict, low-confidence, unavailable, insufficient-evidence, and warning-only floor states."
    - "Playwright uses seeded disposable `/jobs` data and proves compensation evidence stays out of sorting, filtering, Apply concerns, readiness, handoff mutation, and dispatch controls."
    - "Frontend fixtures expose compensation states from projection-shaped data only."
  artifacts:
    - path: "apps/web/src/test/fixtures/projections.ts"
      provides: "Source-conflict and low-confidence compensation fixtures"
      exports: ["makeSourceConflictMarketCompensationAudit"]
    - path: "apps/web/src/views/jobs/JobDetailDrawer.test.tsx"
      provides: "Drawer source-conflict visibility and warning-only boundary regression"
      contains: "renders source-conflict compensation warnings only inside compensation audit"
    - path: "apps/web/e2e/tests/jobs-drawer.spec.ts"
      provides: "Seeded browser release path for source-conflict warning visibility"
      contains: "source_conflict_with_posted_salary"
  key_links:
    - from: "apps/web/src/test/fixtures/projections.ts"
      to: "apps/web/src/views/jobs/JobDetailDrawer.test.tsx"
      via: "projection-shaped fixture builder"
      pattern: "makeSourceConflictMarketCompensationAudit"
    - from: "apps/web/e2e/tests/jobs-drawer.spec.ts"
      to: "/jobs"
      via: "Playwright seeded SQLite product path"
      pattern: "JOBHUNTER_E2E_DB_PATH"
---

<objective>
Close visible frontend/product-path safety evidence for Phase 22.

Purpose: QA-02, QA-05, and QA-06 require browser-visible proof that compensation states render in the Jobs list/drawer while staying warning-only and synthetic. This implements D-02, D-04, D-05, D-06, D-07, D-09, D-10, D-11, D-12, D-14.
Output: Focused web fixture, Jobs list/drawer, and Playwright tests for source conflict, low confidence, explicit unavailable/insufficient states, and prohibited-action boundaries.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-product-path-qa-safety-release/22-VALIDATION.md
@.planning/phases/22-product-path-qa-safety-release/22-CONTEXT.md
@.planning/phases/22-product-path-qa-safety-release/22-RESEARCH.md
@.planning/phases/22-product-path-qa-safety-release/22-PATTERNS.md
@.planning/phases/22-product-path-qa-safety-release/22-UI-SPEC.md
@apps/web/src/test/fixtures/projections.ts
@apps/web/src/views/jobs/JobsView.test.tsx
@apps/web/src/views/jobs/JobDetailDrawer.test.tsx
@apps/web/src/views/jobs/JobCompensationAuditSection.tsx
@apps/web/e2e/tests/jobs-drawer.spec.ts
@docs/local-reliability-qa.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-conflict and low-confidence Jobs unit coverage</name>
  <files>apps/web/src/test/fixtures/projections.ts, apps/web/src/views/jobs/JobsView.test.tsx, apps/web/src/views/jobs/JobDetailDrawer.test.tsx, apps/web/src/views/jobs/JobCompensationAuditSection.tsx</files>
  <read_first>
    - `apps/web/src/test/fixtures/projections.ts`
    - `apps/web/src/views/jobs/JobsView.test.tsx`
    - `apps/web/src/views/jobs/JobDetailDrawer.test.tsx`
    - `apps/web/src/views/jobs/JobCompensationAuditSection.tsx`
    - `.planning/phases/22-product-path-qa-safety-release/22-UI-SPEC.md`
    - `.planning/phases/22-product-path-qa-safety-release/22-PATTERNS.md`
  </read_first>
  <behavior>
    - Fixture: `makeSourceConflictMarketCompensationAudit` returns a projection-shaped recorded market estimate with `source_conflict_with_posted_salary`, `reported_compensation_sample`, safe synthetic source snapshots, and no local/private/provider payloads.
    - Drawer test: `renders source-conflict compensation warnings only inside compensation audit` asserts source-conflict warning code/message appear inside the `Compensation audit` region and do not appear in `Why this job is here`, Apply concerns, missing prerequisites, blockers, Apply readiness, or dispatch controls.
    - Jobs list test: `shows source-conflict warning count without sort filter or query behavior` asserts the warning count is visible from `JobSummary.compensationSummary` and compensation fields are absent from sort buttons, filter buttons, route search, and Operations query arguments.
  </behavior>
  <action>Add the fixture and tests above using existing fixture-builder, TanStack memory router, `buildProviderHarness`, `buildTestPorts`, and MSW drawer patterns. Keep the UI copy from `22-UI-SPEC.md`: source conflict is a warning-only compensation state and must not use destructive styling or imply rejection. If the RED tests show the drawer does not render market warnings, update `JobCompensationAuditSection.tsx` to render existing warning codes/messages from `compensationAudit.market.estimate.warnings` inside the compensation audit surface only. Do not add compensation filters, sorts, route search fields, query params, Apply Review concern text, dispatch controls, new packages, or direct API calls per D-07 and D-09.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web exec vitest run src/views/jobs/JobsView.test.tsx src/views/jobs/JobDetailDrawer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `makeSourceConflictMarketCompensationAudit` uses synthetic sources and contains `source_conflict_with_posted_salary`.
    - Drawer source-conflict warning is visible only inside `Compensation audit`.
    - Jobs list warning count is visible and display-only.
    - No new compensation sort, filter, route search, query field, Apply concern, readiness label, blocker, or dispatch/apply control is added.
  </acceptance_criteria>
  <done>QA-05 has explicit unit-level visible-state coverage and QA-02 warning-only UI boundaries remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Playwright release path evidence for source conflict and safety boundaries</name>
  <files>apps/web/e2e/tests/jobs-drawer.spec.ts</files>
  <read_first>
    - `apps/web/e2e/tests/jobs-drawer.spec.ts`
    - `apps/web/src/test/fixtures/projections.ts`
    - `docs/local-reliability-qa.md`
    - `.planning/phases/22-product-path-qa-safety-release/22-VALIDATION.md`
    - `.planning/phases/22-product-path-qa-safety-release/22-UI-SPEC.md`
  </read_first>
  <behavior>
    - E2E seed uses only `JOBHUNTER_E2E_DB_PATH` disposable SQLite data and synthetic posted/market compensation rows.
    - Product path shows `/jobs` list `Posted`, `Market`, and `Warnings` columns, narrow horizontal scroll, drawer `Compensation audit`, source trail, confidence factors, source conflict, missing/unavailable states, and warning-only copy.
    - Product path asserts no compensation sort/filter buttons, no compensation language in Apply readiness, no compensation warning labels in triage/Apply concerns, and Apply Review handoff preserves selected job without submitting an application.
  </behavior>
  <action>Extend `seedSyntheticCompensationData` in `apps/web/e2e/tests/jobs-drawer.spec.ts` so the existing Director/Platform synthetic job includes `source_conflict_with_posted_salary` in market warnings and the expected list warning count reflects that seeded row. Add Playwright assertions that the source-conflict warning appears in the drawer compensation audit, source warnings stay out of the triage region, the Apply Review handoff still carries only `jobKey`, and no browser submission, auto-apply, mailbox, material regeneration, provider scraping, destructive DB/profile action, or worker-backed apply job is invoked. Keep the test within the existing Jobs drawer spec and do not use real `~/.jobhunter` data per D-02, D-07, and D-11.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web e2e -- tests/jobs-drawer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Playwright verifies `source_conflict_with_posted_salary` is visible in the compensation audit.
    - Playwright verifies compensation source/warning labels stay out of `Why this job is here` and Apply readiness.
    - Playwright verifies compensation sort/filter controls are absent.
    - The e2e seed uses disposable synthetic data through `JOBHUNTER_E2E_DB_PATH` only.
  </acceptance_criteria>
  <done>QA-01, QA-02, QA-05, and QA-06 have seeded browser-path evidence for the release safety boundary.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | Created By |
|----------|------|------------|
| `makeSourceConflictMarketCompensationAudit` | TypeScript fixture helper | Plan 22-03 Task 1 |
| `renders source-conflict compensation warnings only inside compensation audit` | Web unit test | Plan 22-03 Task 1 |
| `shows source-conflict warning count without sort filter or query behavior` | Web unit test | Plan 22-03 Task 1 |
| `source_conflict_with_posted_salary` Playwright assertion | E2E assertion | Plan 22-03 Task 2 |
| Updated `seedSyntheticCompensationData` source-conflict row | E2E fixture data | Plan 22-03 Task 2 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projection-shaped test fixtures -> React views | Fixture data drives visible Jobs list/drawer compensation claims. |
| Disposable e2e SQLite -> browser UI | Seeded local rows drive Playwright product-path evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege / Safety | Playwright product path | mitigate | Use seeded disposable data only; assert no browser submission, auto-apply, mailbox scan, material regeneration, provider call, destructive local action, or worker-backed apply job is part of the spec. |
| T-22-02 | Tampering | Jobs list/drawer compensation UI | mitigate | Unit and e2e tests assert compensation stays display-only and absent from sort, filter, route search, query args, Apply concerns, readiness, blockers, handoff mutation, and dispatch/apply controls. |
| T-22-03 | Information Disclosure | Frontend fixtures and e2e seed rows | mitigate | Fixture helper and e2e rows use synthetic values and safe source snapshots; do not include real profile data, resumes, logs, SQLite contents, credentials, OAuth data, local paths, or provider payloads. |
| T-22-04 | Tampering | Frontend projection consumption | mitigate | Tests consume `JobSummary.compensationSummary` and `JobDetail.compensationAudit`; do not compute salary claims from raw text in React. |
| T-22-SC | Tampering | UI packages and registry blocks | mitigate | `22-UI-SPEC.md` forbids new packages and registry blocks; stop if an install is proposed. |
</threat_model>

<verification>
Run `corepack pnpm --filter @jobhunter/web exec vitest run src/views/jobs/JobsView.test.tsx src/views/jobs/JobDetailDrawer.test.tsx`, `corepack pnpm --filter @jobhunter/web e2e -- tests/jobs-drawer.spec.ts`, `corepack pnpm web:check`, and `git diff --check`.
</verification>

<success_criteria>
Plan 22-03 succeeds when the visible Jobs path proves source-conflict and weak compensation evidence remains inspectable, synthetic, and warning-only across unit and Playwright checks.
</success_criteria>

<output>
Create `.planning/phases/22-product-path-qa-safety-release/22-03-SUMMARY.md` when done.
</output>
