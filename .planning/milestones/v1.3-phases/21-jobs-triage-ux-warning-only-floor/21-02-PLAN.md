---
phase: 21-jobs-triage-ux-warning-only-floor
plan: "02"
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py
  - workers/automation/tests/test_projection_builder.py
autonomous: true
requirements:
  - UI-03
  - UI-04
user_setup: []
must_haves:
  truths:
    - "D-10/D-11/D-12/D-13/D-14: Python projection output matches TypeScript floor comparison semantics, uses only numeric profile floor data, and preserves warning-only behavior."
    - "D-15/D-16: Python projection keeps legacy raw salary compatibility separate from posted and market structured compensation data."
    - "D-17/D-18: parity fixtures include no provider fetch/cache/import path and no unsafe provider/profile/local payload data."
  artifacts:
    - path: workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py
      provides: "Python parity implementation for compensation floorComparison summary/audit JSON"
    - path: workers/automation/tests/test_projection_builder.py
      provides: "Parity tests for configured floor, unconfigured floor, no comparable basis, and warning count"
  key_links:
    - from: workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py
      to: packages/contracts/src/schemas.ts
      via: "matching JSON field names created by Plan 21-01"
      pattern: "floorComparison"
---

<objective>
Add Python projection parity for the compensation floor comparison fields introduced by Plan 21-01.

Purpose: Phase 20 established TypeScript/Python projection parity. The new warning-only floor comparison must preserve that parity before the Jobs UI relies on projected list/detail compensation fields.
Output: Python projection builder support and pytest coverage for the same safe `floorComparison` semantics as the TypeScript projection.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-CONTEXT.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-RESEARCH.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-PATTERNS.md
@.planning/phases/20-canonical-read-model-realtime-api/20-01-SUMMARY.md
@.planning/phases/20-canonical-read-model-realtime-api/20-02-SUMMARY.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-01-SUMMARY.md
</context>

## Artifacts This Phase Produces

- Python projection helper identifiers in `projection_builder.py`: `build_compensation_floor_comparison`, `parse_numeric_profile_compensation_minimum`, and `compare_compensation_range_to_floor`.
- Python parity tests proving the JSON fields match the TypeScript contract from `21-01`.
- Later UI artifacts listed in `21-01` remain downstream of this parity layer.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Python parity tests for floor comparison</name>
  <files>workers/automation/tests/test_projection_builder.py</files>
  <read_first>
    @workers/automation/tests/test_projection_builder.py
    @workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-01-SUMMARY.md
    @.planning/phases/20-canonical-read-model-realtime-api/20-01-SUMMARY.md
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Python compensation summary/audit JSON includes `floorComparison.basis = "posted_salary_basis"` and increments warning count when numeric profile floor exceeds comparable posted compensation.
    - Test 2: Python output uses `basis = "both_posted_and_market"` when both comparable values are below the configured floor and keeps posted/market comparison details separate per D-13/D-16.
    - Test 3: Python output uses `basis = "floor_not_configured"` with zero floor warning count when no numeric floor exists per D-12.
    - Test 4: Python output uses `basis = "no_comparable_compensation_basis"` when structured posted and market values cannot be compared; it does not fall back to raw `jobs.salary` per D-15.
  </behavior>
  <action>Add RED tests in the existing projection builder test module using synthetic local rows. Mirror the exact field names, string union values, warning labels, and warning-count semantics introduced by Plan 21-01. Test that free-text salary expectations are ignored and that no raw benchmark page, credential, local path, or private profile payload appears in the JSON per D-11/D-18.</action>
  <verify>
    <automated>uv --project workers/automation run --extra dev pytest -q workers/automation/tests/test_projection_builder.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail before `projection_builder.py` is updated, and expected JSON names match the TypeScript contract from Plan 21-01 rather than inventing Python-only names.</acceptance_criteria>
  <done>Python parity expectations exist before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Python floor comparison parity</name>
  <files>workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py</files>
  <read_first>
    @workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py
    @workers/automation/tests/test_projection_builder.py
    @packages/contracts/src/schemas.ts
    @apps/api/src/projections.ts
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-01-SUMMARY.md
  </read_first>
  <behavior>
    - Python summary and audit JSON are field-for-field compatible with the TypeScript `floorComparison` contract.
    - Warning count is `postedWarnings + marketWarnings + floorWarningCount`, with floor warning count zero when the floor is not numeric or no comparable structured compensation exists.
    - No ranking, filtering, apply readiness, hard blocker, or dispatch data is changed by this projection-only update per D-05/D-14.
  </behavior>
  <action>Implement Python helpers with the same semantics as the TypeScript helpers from Plan 21-01. Use only the persisted numeric compensation minimum; do not parse free-text salary expectations and do not use legacy raw salary as a normalized basis per D-11/D-15. Preserve posted and market separation in the emitted JSON per D-16. Keep the change inside projection JSON construction and tests; do not add new packages, provider access, source imports, network calls, CLI triggers, workflow activities, apply audit fields, or dispatch logic per D-14/D-17.</action>
  <verify>
    <automated>uv --project workers/automation run --extra dev pytest -q workers/automation/tests/test_projection_builder.py workers/automation/tests/test_posted_compensation_repository.py workers/automation/tests/test_market_compensation_repository.py -q && uv --project workers/automation run --extra dev ruff check workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py workers/automation/tests/test_projection_builder.py</automated>
  </verify>
  <acceptance_criteria>Python projection JSON matches the TypeScript shape, passes focused parity tests, and keeps the floor comparison warning-only with no provider or apply-path side effects.</acceptance_criteria>
  <done>Worker projection parity for floor comparison is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite profile/job rows -> Python projection JSON | Local profile floor and compensation facts become read-model JSON. |
| Python projection JSON -> TypeScript API projection parity | Python and TypeScript builders must agree on visible compensation fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | Python projection fixtures/output | mitigate | Tests assert no free-text salary expectation, credentials, local paths, raw benchmark pages, or unsafe provider payloads enter `floorComparison` per D-11/D-18. |
| T-21-02 | Tampering | Python warning count | mitigate | Warning-count tests include floor warnings only as compensation warnings and never alter apply/scoring/ranking code paths per D-05/D-14. |
| T-21-03 | Spoofing | Python basis state | mitigate | Emit the same explicit basis union as TypeScript: posted, market, both, no comparable basis, floor not configured per D-13. |
| T-21-04 | Denial of Usability | Python missing states | mitigate | Emit explicit unavailable/no-comparable states so UI does not render blanks for missing floor basis per D-12. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package install, upgrade, or dependency refresh is allowed in this plan. |
</threat_model>

<verification>
Run the Python focused pytest and Ruff commands. Confirm `git diff -- workers/automation/src/jobhunter/infrastructure/projections/projection_builder.py` contains projection JSON changes only and no workflow/apply/provider changes.
</verification>

<success_criteria>
Python and TypeScript projection builders produce matching floor comparison JSON from the same canonical fixture semantics, preserving UI-03 and UI-04 without changing product gates.
</success_criteria>

<output>
Create `.planning/phases/21-jobs-triage-ux-warning-only-floor/21-02-SUMMARY.md` when done.
</output>
