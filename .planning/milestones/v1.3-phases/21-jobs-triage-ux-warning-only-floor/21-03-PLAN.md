---
phase: 21-jobs-triage-ux-warning-only-floor
plan: "03"
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - apps/web/src/test/fixtures/projections.ts
  - apps/web/src/views/jobs/JobsView.test.tsx
  - apps/web/src/views/jobs/columns.tsx
  - apps/web/src/styles/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-05
  - UI-06
user_setup: []
must_haves:
  truths:
    - "D-01/D-02/D-16: Jobs list has separate display-only Posted, Market, and Warnings columns, with posted salary treated as employer claim and market estimate as comparison context."
    - "D-03/D-09: compact table dashes are visually minimal but have accessible labels, while drawer plans provide explicit missing-state detail."
    - "D-04/D-06: narrow layouts keep separate columns through horizontal scroll; the drawer audit remains a later sibling section after JobAuditTriage."
    - "D-05/D-14: compensation cells and warning counts do not add sorting, filtering, ranking, fit-score, apply-readiness, blocker, Apply concern, or dispatch behavior."
    - "D-12/D-15/D-18: null summaries and no numeric floor render safely, do not derive normalized data from JobSummary.salary, and expose no unsafe local/provider/profile payloads."
  artifacts:
    - path: apps/web/src/test/fixtures/projections.ts
      provides: "Synthetic compensation summary and floor comparison fixtures for list scan states"
    - path: apps/web/src/views/jobs/JobsView.test.tsx
      provides: "Jobs list tests for compensation columns, market confidence, accessible dashes, warning counts, and no sort/filter behavior"
    - path: apps/web/src/views/jobs/columns.tsx
      provides: "PostedCompensationCell, MarketCompensationCell, CompensationWarningsCell, and three DataGridColumn entries"
    - path: apps/web/src/styles/globals.css
      provides: "Narrow job-compensation table-cell styles and wider Jobs table min-width"
  key_links:
    - from: apps/web/src/views/jobs/columns.tsx
      to: packages/contracts/src/schemas.ts
      via: "JobSummary.compensationSummary"
      pattern: "compensationSummary"
    - from: apps/web/src/views/jobs/JobsView.test.tsx
      to: apps/web/src/views/jobs/columns.tsx
      via: "rendered column/header assertions"
      pattern: "Posted|Market|Warnings"
---

<objective>
Add the Jobs list compensation scan: separate `Posted`, `Market`, and `Warnings` columns backed by structured `JobSummary.compensationSummary`.

Purpose: Users can scan compensation evidence without opening the drawer, while salary data remains display-only and warning-only.
Output: Synthetic fixtures, Jobs list tests, compact accessible table cells, and responsive horizontal-scroll styling.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-CONTEXT.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-RESEARCH.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-VALIDATION.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-PATTERNS.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-UI-SPEC.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-01-SUMMARY.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-02-SUMMARY.md
</context>

## Artifacts This Phase Produces

- `makeCompensationSummary`, `makeCompensationAudit`, `makeCompensationFloorComparison`, `jobWithCompensation`, and `jobDetailWithCompensation` fixture helpers in `apps/web/src/test/fixtures/projections.ts`.
- `PostedCompensationCell`, `MarketCompensationCell`, and `CompensationWarningsCell` in `apps/web/src/views/jobs/columns.tsx`.
- `Posted`, `Market`, and `Warnings` DataGrid column definitions in `jobColumns()`.
- `.job-compensation-*` table styles and widened `.jobs-data-grid-table` min width.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 fixture and RED tests for Jobs list scan</name>
  <files>apps/web/src/test/fixtures/projections.ts, apps/web/src/views/jobs/JobsView.test.tsx</files>
  <read_first>
    @apps/web/src/test/fixtures/projections.ts
    @apps/web/src/views/jobs/JobsView.test.tsx
    @apps/web/src/views/jobs/columns.tsx
    @apps/web/src/views/jobs/JobsView.tsx
    @apps/web/src/shared/ui/filterable-data-grid.tsx
    @packages/contracts/src/schemas.ts
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-VALIDATION.md
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Jobs list renders separate `Posted`, `Market`, and `Warnings` headers and cells from synthetic `JobSummary.compensationSummary` per D-01/D-16.
    - Test 2: parsed posted range displays in `Posted`; market `estimated_range`, `unsupported`, `insufficient_evidence`, `source_unavailable`, and `not_requested` states render distinct compact scan labels per UI-01/UI-05.
    - Test 2a: the `Market` cell renders a compact statistical confidence indicator from `row.compensationSummary.market.confidenceBand` plus `sourceCount` when available, such as `medium confidence` / `4 sources`, so users can scan confidence without opening the drawer per UI-01.
    - Test 3: null `compensationSummary` and missing/unsupported values render visual `-` with accessible names such as `No posted salary recorded` and `Market estimate unsupported` per D-03.
    - Test 4: warning count renders text such as `2 warnings`; floor warnings are absent when `floorComparison.basis = "floor_not_configured"` per D-12.
    - Test 5: no compensation column exposes a sort button, filter menu, route search field, API filter body, or bulk-selection side effect per D-05/D-14.
  </behavior>
  <action>Add safe synthetic fixtures first. Extend `apps/web/src/test/fixtures/projections.ts` with named builders for populated posted+market compensation, null summary, no posted salary, unsupported market, insufficient evidence, source unavailable, and floor configured/not configured states. Fixture builders must allow market `confidenceBand` values `none`, `low`, `medium`, and `high`, plus `sourceCount` values such as 0, 2, and 6. Then add RED assertions in `JobsView.test.tsx` using existing router/provider harnesses and Testing Library role/name queries. Tests must prove the `Market` column exposes statistical confidence scan text from `row.compensationSummary.market.confidenceBand` and `sourceCount` when present, while still rendering explicit unsupported/missing state labels. Keep fixture data synthetic and omit credentials, local paths, raw provider pages, source payloads, and private profile text per D-18. Do not change production UI in this task.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/views/jobs/JobsView.test.tsx</automated>
  </verify>
  <acceptance_criteria>The new tests fail before the column implementation because the `Posted`, `Market`, and `Warnings` columns/cells do not exist yet; fixture builders typecheck against the shared contract from Plan 21-01; at least one failing assertion requires visible `Market` confidence text derived from `confidenceBand` and `sourceCount`.</acceptance_criteria>
  <done>Wave 0 list fixtures and tests exist before the Jobs list implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement display-only compensation columns and responsive table sizing</name>
  <files>apps/web/src/views/jobs/columns.tsx, apps/web/src/styles/globals.css</files>
  <read_first>
    @apps/web/src/views/jobs/columns.tsx
    @apps/web/src/views/jobs/JobsTable.tsx
    @apps/web/src/views/jobs/JobsView.tsx
    @apps/web/src/shared/ui/filterable-data-grid.tsx
    @apps/web/src/styles/globals.css
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-PATTERNS.md
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-UI-SPEC.md
  </read_first>
  <behavior>
    - `jobColumns()` includes `Posted`, `Market`, and `Warnings` after `Sources` and before `Location` unless implementation constraints require a nearby placement; the columns are not sortable/filterable.
    - `PostedCompensationCell` and `MarketCompensationCell` read only `row.compensationSummary`, never parse `row.salary` or source text per D-15.
    - `MarketCompensationCell` shows both estimate state/range and a compact confidence indicator from `row.compensationSummary.market.confidenceBand` plus `sourceCount`; recorded estimates expose values such as `high confidence` and `6 sources`, while non-recorded states expose `no confidence` or an equivalent accessible label per UI-01.
    - `.jobs-data-grid-table` remains horizontally scrollable on narrow screens and is widened enough for the added columns per D-04/UI-06.
  </behavior>
  <action>Implement view-local table cell helpers in `columns.tsx` using existing `DataGridColumn<JobSummary>`, `TitleStack`, `.tag`, and dense table patterns. Render compact dashes through a helper with `aria-label` and `title` for missing states per D-03. Posted cell prefers structured posted display range. Market cell renders estimated range or compact state labels without false precision and also renders a compact confidence line/tag from `row.compensationSummary.market.confidenceBand` and `row.compensationSummary.market.sourceCount`; examples include `high confidence`, `medium confidence`, `low confidence`, `no confidence`, and `6 sources`. The confidence text must be visible or included in an accessible name within the `Market` cell, not only hidden in the drawer. Warnings cell renders text count and includes floor warning count only when it is already present in `row.compensationSummary.warningCount` from the projection per D-10/D-14. Do not add `sortable: true`, compensation `getFilterValue`, `SORTABLE_JOB_FIELDS`, `SEARCH_FILTER_COLUMNS`, route search params, API query params, fit-score factors, Apply concerns, blockers, readiness flags, or dispatch conditions per D-05/D-14. Add only narrow `.job-compensation-*` CSS and adjust `.jobs-data-grid-table` min width; preserve existing horizontal scroll rather than collapsing columns per D-04.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/web test src/views/jobs/JobsView.test.tsx && corepack pnpm web:check && git diff --check</automated>
  </verify>
  <acceptance_criteria>Jobs list shows posted, market state/range, market statistical confidence (`confidenceBand` plus `sourceCount`), and warning scan data from `compensationSummary`; compact dashes are accessible; no compensation sort/filter/ranking/apply behavior is introduced; table layout remains horizontally scrollable with stable cell widths.</acceptance_criteria>
  <done>UI-01, UI-03, UI-05, and the list/table portion of UI-06 are implemented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API `JobSummary` -> Jobs table | Projection-backed compensation summary becomes visible table text. |
| Synthetic fixtures -> tests | Test data must remain safe and non-sensitive. |
| Table controls -> route/API query | New columns sit near sort/filter controls but must not become product gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `apps/web/src/test/fixtures/projections.ts` and table cells | mitigate | Use only synthetic safe DTO fields; never expose local paths, credentials, private profile text, raw provider pages, or unsafe source payloads per D-18. |
| T-21-02 | Tampering | `jobColumns()` and `JobsView` route/filter state | mitigate | Tests assert no compensation sort/filter controls and no compensation route/API query fields per D-05/D-14. |
| T-21-03 | Spoofing | Market estimate scan labels | mitigate | Render unsupported/insufficient/source-unavailable states explicitly instead of precise-looking ranges per UI-05. |
| T-21-04 | Denial of Usability | Compact dashes and narrow table layout | mitigate | Accessible dash labels and horizontal scroll/min-width tests/styles preserve usability per D-03/D-04/UI-06. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package install, upgrade, or dependency refresh is allowed in this plan. |
</threat_model>

<verification>
Focused list verification: `corepack pnpm --filter @jobhunter/web test src/views/jobs/JobsView.test.tsx`, `corepack pnpm web:check`, and `git diff --check`.
</verification>

<success_criteria>
Users can scan posted salary, market estimate state/confidence label, and warning count from the Jobs list without opening the drawer, and the new columns remain display-only.
</success_criteria>

<output>
Create `.planning/phases/21-jobs-triage-ux-warning-only-floor/21-03-SUMMARY.md` when done.
</output>
