---
phase: 21-jobs-triage-ux-warning-only-floor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/schemas.ts
  - apps/api/src/projections.ts
  - apps/api/test/projections.test.ts
  - apps/api/test/server.test.ts
  - docs/local-ts-api.md
autonomous: true
requirements:
  - UI-03
  - UI-04
user_setup: []
must_haves:
  truths:
    - "D-10/D-11/D-12/D-13/D-14: profile-floor comparison exists as warning-only compensation audit data, uses only numeric profile compensation minimum, names posted/market/both/neither basis, and never changes ranking, filters, fit score, apply readiness, blockers, or dispatch."
    - "D-05/D-14: no salary sort field, salary filter field, ranking input, apply prerequisite, hard blocker, Apply concern, or dispatch gate is added by the floor comparison DTO."
    - "D-15/D-16: JobSummary.salary remains raw compatibility data, while posted compensation facts and market estimates stay separate in structured compensation summary/audit data."
    - "D-17/D-18: the DTO and tests add no Levels.fyi/Glassdoor provider access and expose no credentials, local paths, private profile text, raw benchmark pages, or unsafe provider payloads."
  artifacts:
    - path: packages/contracts/src/schemas.ts
      provides: "JobCompensationFloorComparison and basis/state types embedded in JobCompensationSummary and JobCompensationAudit"
    - path: apps/api/src/projections.ts
      provides: "Projection-owned floor comparison computation included in compensation warningCount"
    - path: apps/api/test/projections.test.ts
      provides: "API projection regressions for numeric floor, basis, warnings, and no configured floor"
    - path: apps/api/test/server.test.ts
      provides: "Boundary regression proving compensation floor data stays out of sort/filter/apply behavior"
    - path: docs/local-ts-api.md
      provides: "Narrow documentation for additive floor comparison read-model fields"
  key_links:
    - from: apps/api/src/projections.ts
      to: packages/contracts/src/schemas.ts
      via: "compensation summary/audit JSON shape"
      pattern: "floorComparison"
    - from: apps/api/test/server.test.ts
      to: apps/api/src/projections.ts
      via: "boundary test over jobs list and jobs detail API responses"
      pattern: "compensation.*boundary"
---

<objective>
Add the minimal safe read-model contract for profile-floor comparison so Phase 21 can show warning-only floor evidence and include floor warnings in the Jobs list warning count without React-side profile parsing.

Purpose: Floor comparison is required by UI-03 and UI-04, but it must be owned by the projection/API contract rather than by view-level parsing.
Output: Shared TypeScript DTO, TypeScript projection computation, API boundary tests, and local API docs for the additive floor comparison fields.
</objective>

<execution_context>
@/Users/eloibarti/.codex/gsd-core/workflows/execute-plan.md
@/Users/eloibarti/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-CONTEXT.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-RESEARCH.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-VALIDATION.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-PATTERNS.md
@.planning/phases/21-jobs-triage-ux-warning-only-floor/21-UI-SPEC.md
@.planning/phases/20-canonical-read-model-realtime-api/20-01-SUMMARY.md
@.planning/phases/20-canonical-read-model-realtime-api/20-02-SUMMARY.md
</context>

## Artifacts This Phase Produces

- `JobCompensationFloorComparison` plus basis/state string unions in `packages/contracts/src/schemas.ts`.
- `floorComparison` fields on both `JobCompensationSummary` and `JobCompensationAudit`.
- Projection helper identifiers in `apps/api/src/projections.ts`: `buildCompensationFloorComparison`, `parseNumericProfileCompensationMinimum`, and `compareCompensationRangeToFloor`.
- View symbols in later plans: `PostedCompensationCell`, `MarketCompensationCell`, `CompensationWarningsCell`, `JobCompensationAuditSection`, `CompensationMetric`, and `CompensationDisclosure`.
- Synthetic fixture builders in later plans: `makeCompensationSummary`, `makeCompensationAudit`, `makeCompensationFloorComparison`, `jobWithCompensation`, and `jobDetailWithCompensation`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the floor comparison contract with failing tests</name>
  <files>packages/contracts/src/schemas.ts, apps/api/test/projections.test.ts, apps/api/test/server.test.ts</files>
  <read_first>
    @packages/contracts/src/schemas.ts
    @apps/api/src/projections.ts
    @apps/api/test/projections.test.ts
    @apps/api/test/server.test.ts
    @apps/api/src/profile-store.ts
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-CONTEXT.md
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: a job with numeric `profile.compensation.salary_range_min` and below-floor posted compensation produces `floorComparison.basis = "posted_salary_basis"`, `warningCount` includes one floor warning, and the warning is compensation-only per D-10/D-11/D-14.
    - Test 2: a job with below-floor posted and market compensation produces `basis = "both_posted_and_market"` and separate posted/market comparison arms per D-13/D-16.
    - Test 3: a job with no numeric profile compensation minimum produces `basis = "floor_not_configured"`, no floor warning, and no list warning-count contribution per D-12.
    - Test 4: existing compensation boundary tests prove compensation fields do not affect sort/filter/ranking/fit-score/apply readiness/blockers/dispatch per D-05/D-14.
  </behavior>
  <action>Add RED tests before implementation. Extend existing compensation projection tests instead of adding a new harness. Use synthetic rows only. The contract target is a safe `JobCompensationFloorComparison` object embedded as `floorComparison` in both `JobCompensationSummary` and `JobCompensationAudit`; it may expose numeric floor amount, currency, annual period, basis, posted comparison, market comparison, warning labels, and warning count. It must not expose `salary_expectation`, free-text profile preferences, credentials, local paths, source pages, or provider payloads per D-11/D-18. Add server boundary assertions that no compensation field is accepted as a sort/filter field and that apply audit/readiness/dispatch responses are unchanged per D-05/D-14.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/api exec vitest run test/projections.test.ts -t "compensation" && corepack pnpm --filter @jobhunter/api exec vitest run test/server.test.ts -t "compensation boundary"</automated>
  </verify>
  <acceptance_criteria>Tests initially fail for missing `floorComparison`, then pass after Task 2; the tests cite numeric floor only and contain no fixture data that resembles secrets, local paths, private profile text, raw benchmark pages, or provider payloads.</acceptance_criteria>
  <done>Floor comparison behavior is specified by failing tests before projection implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TypeScript projection-owned floor comparison</name>
  <files>packages/contracts/src/schemas.ts, apps/api/src/projections.ts, docs/local-ts-api.md</files>
  <read_first>
    @packages/contracts/src/schemas.ts
    @apps/api/src/projections.ts
    @apps/api/src/read-model.ts
    @apps/api/src/profile-store.ts
    @docs/local-ts-api.md
    @.planning/phases/20-canonical-read-model-realtime-api/20-02-SUMMARY.md
    @.planning/phases/21-jobs-triage-ux-warning-only-floor/21-RESEARCH.md
  </read_first>
  <behavior>
    - `buildCompensationSummary` includes posted warnings + market warnings + floor warnings only when a numeric configured floor produces a below-floor concern.
    - `buildCompensationProjection` computes floor comparison from persisted projection inputs and numeric profile minimum; it does not compute salary facts during GET reads.
    - `JobSummary.salary` remains raw compatibility fallback and is not used as normalized table data per D-15.
  </behavior>
  <action>Implement the contract and projection helpers named in the artifacts section. Read only the existing numeric profile compensation minimum from the owning local profile storage data, parsing finite numeric values from `compensation_salary_range_min` or the equivalent profile row field. Ignore `salary_expectation` and all free-text values per D-11. Compare floor against structured posted annual range and market annual range only when those structured values are present and comparable; otherwise emit `no_comparable_compensation_basis` or `floor_not_configured` per D-12/D-13. Keep posted and market comparison arms separate per D-02/D-16. Update `docs/local-ts-api.md` narrowly to document the additive `floorComparison` summary/audit fields and the warning-only boundary per D-05/D-14. Do not add new dependencies, package upgrades, provider fetch paths, profile editing flows, source imports, or read-time salary parsing per D-17.</action>
  <verify>
    <automated>corepack pnpm --filter @jobhunter/contracts check && corepack pnpm api:check && corepack pnpm --filter @jobhunter/api exec vitest run test/projections.test.ts -t "compensation" && corepack pnpm --filter @jobhunter/api exec vitest run test/server.test.ts -t "compensation boundary"</automated>
  </verify>
  <acceptance_criteria>`JobCompensationSummary.floorComparison` and `JobCompensationAudit.floorComparison` are typed, projection-backed, and additive; warning counts include floor concerns only when numeric floor comparison produces a compensation warning; no compensation field is wired into sort/filter/ranking/apply readiness/blockers/dispatch.</acceptance_criteria>
  <done>TypeScript contracts, projection JSON, API boundary tests, and local API docs satisfy UI-03/UI-04 without React-side floor parsing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile store -> projection JSON | Numeric compensation minimum is local profile data crossing into job read-model JSON. |
| canonical compensation rows -> API response | Posted/market compensation evidence crosses into user-visible list/detail DTOs. |
| API response -> browser UI | Warning-only compensation data crosses into Jobs triage rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | `JobCompensationFloorComparison` DTO | mitigate | Expose only safe numeric floor comparison facts; exclude free-text salary expectations, private profile text, local paths, credentials, raw benchmark pages, and unsafe provider payloads per D-11/D-18. |
| T-21-02 | Tampering | `apps/api/src/projections.ts` warningCount | mitigate | Tests prove floor warnings affect only compensation warning count and never sort/filter/ranking/apply readiness/blockers/dispatch per D-05/D-14. |
| T-21-03 | Spoofing | Floor basis labels | mitigate | Emit explicit basis values for posted, market, both, no comparable basis, and floor not configured per D-13. |
| T-21-04 | Denial of Usability | Missing floor state | mitigate | Return explicit `floor_not_configured` and `no_comparable_compensation_basis` states so the UI does not show blank or misleading data per D-12. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No package install, upgrade, or registry command is allowed in this plan. |
</threat_model>

<verification>
Run the focused API/contract commands in task verification. Before handing off to UI plans, confirm `git diff -- packages/contracts/src/schemas.ts apps/api/src/projections.ts` contains only additive compensation fields and no sort/filter/apply dispatch changes.
</verification>

<success_criteria>
UI-03 and UI-04 have a safe projection-owned contract: profile-floor comparison is inspectable, basis-labeled, warning-only, and unavailable when no numeric floor exists.
</success_criteria>

<output>
Create `.planning/phases/21-jobs-triage-ux-warning-only-floor/21-01-SUMMARY.md` when done.
</output>
