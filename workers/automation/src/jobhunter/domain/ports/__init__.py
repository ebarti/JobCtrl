"""Domain ports — abstract interfaces that the domain depends on.

Ports define the contracts between the domain layer and infrastructure.
Adapters in ``jobhunter.infrastructure`` provide concrete implementations.
"""
